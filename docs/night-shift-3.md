# FACTORY DRIVER — NIGHT SHIFT 3 LOG (2026-09-22)
# Honest record of the shift: what was done, what remains. Companion docs: docs/design/,
# docs/art-direction.md. The detailed user-facing report lives outside the repo (project scratch).

## VERIFIED & PUSHED (every iteration double-gated: external job + orchestrator + independent re-run; suite byte-identical; certs byte-locked; zero errors)

| It | What | Commits | HEAD after |
|----|------|---------|-----------|
| 3X | Thermal reset semantics: R keeps heat, cools by physics | b445d79 | b445d79 |
| 3W | Mass ledger: 27-row mass table summing to kerb 1300 exactly, 38/62 split | 9b0fb7b | 9b0fb7b |
| 3L | Wear & aging: six accumulators over existing physics, staircase effects | e294ebe | e294ebe |
| 3K | Licence ladder: L0 sitting (7 elements), skid pad, the gate, ranks | c5343c5 | c5343c5 |
| 3Y | R does not refuel: reset keeps fuel like heat/wear (user's 12:55 bug) | 2a4ce24 | 2a4ce24 |
| 4A | Garage/menu on Tab + THE STUDY (21 live lessons, input display) + car condition + licence panel + data location (FD_DATA_DIR, copy-only migration) + HUD bar legend; menu_test step | d48b0cf | d48b0cf |
| 3Z | Licence sitting memory: per-element progress persisted, resume at first failed element, theory never retaken once passed, practice runs change nothing; instructor's car manual->auto after hill start (user's verdicts) | fdf7464 | fdf7464 |
| 4A.1 | UI readability: every surface fits 1280x720; 23 overflow checks walk the UI (the standing visual-defect rule encoded) | 3ba3cea | 3ba3cea |
| 3AA | Caster return like a real car: zero at standstill, none in reverse, speed-gated rolling return; five drivers re-taught to countersteer; certs re-baselined honestly | 9db6638 | 9db6638 |
| 3AC | Airborne honesty: unsupported wheels carry nothing (phantom force removed), ballistic flight, no drive/grip/rolling/hill in the air, full-droop wheels, honest landing; airborne_test step | 1db32ba | 1db32ba |
| 3AB | Wear measured in kilometres: rated lives (clutch 175k, pads 50k, tyres 25.5k/17k, engine 140k km, sourced/labelled estimates) + driving style as metres-of-life on top; equivalences measured (launch ~474 m clutch, stop ~252 m pads, donut ~1.26 km rear, idle ~31 km/h-equiv) | c5ee9c3 | c5ee9c3 |
| 3AD | Landing honesty: premise corrected by measurement — the bug was large-attitude seat extrapolation (266 kN catapult -> tumbling -> springs-flat "cat landing"), NOT the tail hop; exact rigid-body seat geometry above 0.1 rad, the shell as contact (bounded stops + friction), deterministic topple, no guaranteed four-down; the flip (F) rights an overturned car at rest, orientation only | 74aaef5 | 74aaef5 |
| 3AE | Flip visual fix: the level draw now writes the whole wheel/body transform (was: x/z kept the overturned carried picture — side-rest buried the wheels in the body, roof-rest mirrored them); fixed at the draw site so flip, reset and every settle-then-render path are covered; drawn layout asserted to the bit against car.tscn | b0ce888 | e5441d4+ |
| 3AF | Rollover momentum: overturned shell friction capped at ROLL_FRICTION_COEFF 0.45 x weight (kinetic friction of a car on its roof/side) — the CoM decelerates at <= 0.45 g while the roll continues about the moving contact; the wall-pivot is gone (worst per-tick CoM drop 1.15 m/s -> 0.075 m/s; the roll travels 17.4 m, rests on its wheels 43 m on, deterministically); upright shell contact untouched, 3AD numbers held byte-for-byte | 41189fa | 7d642ac |

Certified times at shift end (byte-locked): SLALOM 28.65 (was 28.82 before 3AA's caster; moved honestly, cited) / SPIN_180 15.95 / SPIN_360 18.07 / STOP_BOX 8.72 / REVERSE_180 7.72. Suite: 1281 ok across 13 headless steps.

## THE DRIVER'S CATCHES (all found by playtesting, all recorded with telemetry where given)
- 12:55 R-refuel bug -> fixed in 3Y. 14:56 sitting forgets passed elements -> 3Z. 16:15 STUDY tab off screen + the standing visual-defect rule -> 4A.1.
- ~17:25 ramp jump "wheels and body fell apart, joints stretched" -> 3AC (phantom wheel force). 20:35 retest "wheels stick to their axis... every jump ends up on all 4 wheels, like a cat" -> 3AD (large-attitude seat catapult; shell contact; no guaranteed landing).
- 23:30 flip with no wheels (side-rest: wheels invisible; roof-rest: wheels reversed) -> 3AE.
- 23:50 handbrake rollover "rolled around its axis without letting the lateral force move the car... like the car hit a wall" -> 3AF (rollover must tumble along its momentum, not pivot on a pinned contact).

## IN FLIGHT AT SHIFT CLOSE (honest state)
- 3AE + 3AF (the two catches above): external job editing scripts/car.gd (uncommitted), gates pending — it pushes itself when green. Certs byte-locked throughout.
- Design synthesis draft (art canon x gameplay, the driver's brain dump folded in) filed at docs/design/design-synthesis-draft.md — DRAFT, awaiting the driver's verdict before 4B.

## SHIFT-CLOSE ADDENDUM (00:40, after the user went to bed): 3AE + 3AF LANDED
Both fixes verified (double gate + independent re-run: suite 1292 ok across 3 runs byte-identical modulo the documented pid-path artifact, zero errors, certs byte-locked 28.65/15.95/18.07/8.72/7.72, pushed, HEAD == origin/main == 7d642ac):
- The flip's picture (3AE): the level-draw branch now writes whole transforms — side-rest flip shows all four wheels at their own corners (was: buried in the body, "no tires"), roof-rest flip shows left wheels left and right wheels right (was: mirrored). Fixed at the draw site, so flip, reset and any settle-then-render path are covered at once; asserted to the bit against the scene's layout.
- The roll's momentum (3AF): overturned, the shell's friction is the car's kinetic friction on the road at most (ROLL_FRICTION_COEFF 0.45 — reconstruction practice 0.4-0.5 g), so the centre of mass keeps travelling (decelerated <= 0.45 g) while the roll goes on about a contact that moves with it. The user's exact handbrake maneuver measured: was 1.15 m/s taken off the CoM in one tick (the wall), now 0.075 m/s worst tick; the roll travels 17.4 m while over (physics floor 15.5 m), ends on its wheels 43 m on; the diagonal rollover now slides ~15 m further on its side before resting. Where a roll ends is still the numbers' call — honest.

## DESIGN CANON REGISTERED THIS SHIFT
- docs/art-direction.md — the visual canon (verbatim driver brief; Porsche Unleashed PC 2000 aesthetic; road-is-the-composition; asset priority 1-4).
- docs/design/user-thoughts-economy.org — the driver's brain dump (verbatim): troc economy (goods/services/vouchers), cat ecology (population, scare level, 9 lives = 9 guaranteed landings, cats never die on the Ring, social gas stations), teleport bridges, living traffic (drivers die without a car unless they have a cat; school rigor scales with deaths), home-made cars + registration numbers, OSM skeleton world first, environment-dependent sound.
- The Ring as proving ground: jobs -> materials -> workshop -> build -> test on the Nurburgring -> car.

## WHAT REMAINS (queue, honest)
1. 3AE/3AF gates + push (in flight).
2. Driver's verdict on the design synthesis (open questions: building types, first OSM chunk scope, proving-ground pass rules, car ladder, first teleport destination).
3. 4B-prep: OSM skeleton world (data pipeline researched: docs/nordschleife-data-sources.md — DGM-2 LiDAR + OSM, free-with-attribution) + the Green Hell brief carrying the art canon; gravity-on-slopes physics rides here.
4. Then per the synthesis sequence: cats -> troc economy -> Ring proving ground -> living traffic -> sound -> Blender car pipeline (home-made models; the boxy placeholder is the canon violation to fix).
- Backlog (user-verdict pending or parked): tyre compounds per-compound thermal character; the STUDY's coming-soon lessons (drifting, quick turn need slide-reading pilots; traffic rules need the 4C world); recorded-telemetry scrub playback; 4A garage catalogue-skin pass; OSM continental dream (Phase 4 back end).

## METHOD (unchanged, for the record)
Headless verification only (the driver may be driving); every iteration = orchestrator subagent driving an external claude_code job + double gates + independent re-run; tests additive-only, checks never weakened; every retune documented was-> reason; suite byte-identical twice + --parallel; push only after gates. User canon: "we're not looking for drama, we are looking for real physics simulation."

## 4B-PREP (2026-09-23): THE DESIGN/RESEARCH PACKAGE, LANDED
Docs-only iteration (no game code touched). External job wrote, orchestrator gated (suite 3 runs: twice sequential byte-identical modulo the documented fd-3K-licence pid-path line, once --parallel; 1292 ok each, zero errors, certs byte-locked 28.65/15.95/18.07/8.72/7.72), committed, pushed.
- docs/design/4b/element-library.md — the element list, ordered by the canon's asset priority (R road segments, T terrain, S sky/light, B typed buildings, E economy buildings with privileges, V vegetation, F furniture); every entry: canon quote, OSM/DEM data that instantiates it, what is fixed in stone vs varies per region, the 2000 look.
- docs/design/4b/data-pipeline.md — OSM + DGM1 -> element assembly: extract/project/skeleton/drape/dress/bake, snapshot pinning, exact attributions.
- docs/design/4b/ring-region-decisions.md — the first region put in stone: bbox 50.30-50.45N 6.80-7.10E, origin E0 352000 N0 5577000 (EPSG:25832), OSM snapshot 2026-09-22T08:45:51Z, live counts (2181 drivable ways; relation 38566 = 52 ways, 1119 nodes, 20.7 km), POI inventory (9 fuel, 9 repair, 1 dealer, 1 driving school, 4 castles, 12 grandstands, 11 roundabouts), decisions marked PUT IN STONE / DECIDED / DEFERRED.
- docs/design/4b/first-run-flow.md — pin -> test centre -> licence -> voucher -> rental -> first car, grounded in existing code (licence_manager/study/garage/data_dir/odometer_store cited by line), frozen licence semantics untouched.
- docs/design/4b/implementation-plan.md — 4B-1..4B-8 gated iterations (library -> skeleton -> drape -> drivable Ring -> buildings -> first-run UI -> dressing), each with files, additive tests, headless definition of done; GRAVITY-ON-SLOPES scoping (design-only): slope physics delivered through the existing RoadProfile injection seam (car.gd:2818 export, ramp_gradient 4677, sample_height 4693, the hill step 4130) so car.gd stays untouched and the flat-pad certs stay byte-locked (ramp_gradient is Vector2.ZERO on the flat pad).
- docs/design/4b/samples/ — OSM Karussell extract (3 ways of relation 38566, 4.2 KB) + LICENSE-NOTES.md with the exact OSM ODbL and DGM1 DL-DE BY 2.0 attributions, live-verified with URLs + access date.
Live data findings recorded: DGM1 (1 m, RLP) is now free open data — upgrade on the recorded DGM-2; DGM1 tiles direct HTTPS no account (geobasis-rlp.de, metalink-indexed, SHA-256); touristenfahrten.geojson declares NO licence (verification only, never shipped). Open questions for the driver sit at the end of each doc (road width 5 vs recorded 8-9 m; per-car licence vs the first car's entry; the social station and cat sanctuary picks; DL-DE vs CC BY-SA; DGM1 vs DOM1 for the Karussell bank).
## 4B-1 (2026-09-23): THE ELEMENT CATALOGUE AS DATA, LANDED
First 4B iteration built. External job wrote, orchestrator gated and pushed (commit 7fb5fab, HEAD == origin/main). Suite 1749 ok across five runs — the job's two sequential (byte-identical modulo the documented fd-3K-licence pid-path line at 1679, the only allowed artifact), the orchestrator's two sequential, one --parallel, plus the independent re-run on the git archive HEAD copy of the commit; zero SCRIPT ERROR/ERROR:/Parse Error everywhere; certs byte-locked 28.65/15.95/18.07/8.72/7.72; frozen files' diff empty (car.gd, road_profile.gd, test_pad.gd, asphalt_texture.gd, mission_manager.gd, handling_tests.gd, licence_manager.gd, scenes/, project.godot, configs/cars/, configs/validation.gd); tests additive-only (one new file, run_tests.sh's one step, the README rows); user-thoughts.org untouched.
- configs/elements/*.json — the library of element-library.md transcribed as data, one file per family, 81 entries: R 18 / T 9 / S 6 / B 11 / E 14 / V 8 / F 15. Every entry: id, family, title, priority (the canon's asset priority of its family), the canon line it cites, the OSM/DEM data, what is stone (plus stone_parameters: the numbers and names the library states), what a region table may vary (each parameter with a range or choices and its default inside), its texture budget, and the library's asides as notes.
- scripts/element_catalogue.gd — ElementCatalogue: a class of static functions as CarConfigValidation is, no autoload; read_files(), entry(id), by_family(letter), validate(files) returning one fault line per wrong thing (file, entry, field); the schema constants (id pattern, family priorities 1-9, the privilege vocabulary of the E-table, the canon's texture budgets, the R surface classes with gravel's friction "to be measured"), and nothing that touches the game — no mesh, no scene, no physics reads it yet (4B-5 and later do).
- tests/element_catalogue_test.gd — the suite's new step (after the config test, same family: cheap static data that fails first), 457 ok lines: every entry held to the plan's asks (id of the documented form and unique, canon cited, priority 1-9 and its family's, every varying parameter with its default inside its range or choices, texture inside the family budget, the E-table's shells real B ids and privileges from the vocabulary), the files parsed twice to the same bytes, the counts held to the library's (a dropped or invented entry fails here), entry()/by_family() answering, and validate() on 16 fixtures broken in code naming the entry and the field — never a file on disk broken.
- run_tests.sh's header and the --parallel wording updated (fourteen tests), tests/README.md's enumeration corrected (it had said "twelve" and omitted the airborne test: now says what runs), README.md's Tests prose gains the step's description in house style.

Decisions made on the library's behalf (flagged "chosen for the catalogue" in the JSON; the driver may veto any): where the library names a parameter without numbers, ranges were chosen and flagged (R9 bank 20-40 % bowl 6-12 m flat strip 0.5-2 m; R11 lamp spacing 10-40 m; R13 lanes 2-4; B0 roof pitch 25-55°; B10 gates 2-8; T2 treeline 0-3000 m; S1 sun elevation 0-90°; S6 rain intensity 0-1); defaults where the library gives a range but no default lean to the Eifel or the canon's first-named value (R1 lane width 3.5 m, R3 outside barrier F1, R10/F11 steel, R11/F12 concrete, R12 rock, T3 grey slate, T8 grey-green, B0 white, B5 grey); choice lists hold only what the library names (several single-choice); the S family has no line in the canon's texture budget so its budget is empty and unchecked; E2's social 10 % sell_tuna variant and E5's car storage are notes, not privileges (the vocabulary check covers each entry's base set); E14's shell is the string "any", its privileges the region table's.

## 4B-2 (2026-09-23): THE OSM EXTRACT AND THE RING SKELETON, LANDED
Second 4B iteration built and gated by the external job; the size gate went to the Conductor and the verdict was the full file checked in (the job's brief: a skeleton over 1 MB is the Conductor's decision, not the job's): the honest full-bbox skeleton is 5 953 503 bytes, sha256 b86984bac5ba2b0d686616228fc015f912cb1abf1c82bb5484454926668ef42f, because the bbox holds 9 188 drivable ways at the pinned snapshot, not the 2 181 the design package recorded (ring-region-decisions.md §2 and data-pipeline.md §9 say "2 181 ways"; the attic pull answers 9 188: track 5 444, service 1 536, residential 1 169, tertiary 338, primary 278, secondary 211, raceway 98, unclassified 87, primary_link 15, secondary_link 5, living_street 4, tertiary_link 3 — 2 208 without track and service, so the recorded count was likely of the paved classes; the "was ->" corrections and the open-question-4 decision are in those docs now). Sizes of the honest alternatives, same pipeline, same rounding: all q1 classes 5 953 503 B (16 771 segments, 10 700 junctions, 131 002 points); without track 2 059 781 B; without track and service 1 271 334 B; primary..unclassified + raceway + links 638 208 B; relation 38566 alone 35 731 B. Everything else is complete and gated with the full file in the working tree: suite 1 919 ok (1 749 + the new step's 170) across three runs — twice sequential byte-identical modulo the documented fd-3K-licence pid-path line (line 1853 this time), once --parallel; zero SCRIPT ERROR/ERROR:/Parse Error; certs byte-locked 28.65/15.95/18.07/8.72/7.72 (cmp against the 33-line cert base, exit 0 in all three); frozen files' diff empty; tests additive-only; user-thoughts.org untouched.
- tools/world/extract_osm.py — the six Overpass queries of data-pipeline.md §2.1 verbatim, each with the attic date `[date:"2026-09-22T08:45:51Z"]` in its settings block (q6 composed from the doc's sentence: barrier ways, highway=street_lamp|stop|give_way ways, nwr traffic_sign, power=pole|line ways, nwr railway=level_crossing|rail; flagged "chosen for the skeleton", nothing in 4B-2 reads it), written as .ql files into a snapshot folder named by §7 (`ring_2026-09-22T08-45-51Z`), fetched with curl exactly as §2.1's loop (form-encoded `data@`, UA `factory-driver-4b/0.1 (world pipeline; okaypeace@gmail.com)`, --max-time 300, 10 s apart, three tries per endpoint 30 s apart, lz4 then kumi then overpass-api.de), a manifest.json per snapshot (endpoint, counts by type, bytes, sha256, the server's own timestamp). Measured and recorded in the script: an attic answer's `osm3s.timestamp_osm_base` is the serving database's live time (00:57-01:01Z on 2026-09-23), NOT the attic date; the data is the attic view all the same (q2 answered the recorded 52 ways / 1 119 nodes exactly; a second pull 3 minutes later gave the same skeleton to the byte), so the check is "served at or after the attic date" and the snapshot's identity is the date in the .ql files. Raw pulls, this snapshot, all from lz4.overpass-api.de: q1 9 188 ways (10 892 275 B), q2 1 relation + 52 ways (91 686 B), q3 989 ways + 58 nodes + 2 relations (867 778 B), q5 17 635 ways (12 872 163 B), q6 600 ways + 44 nodes (510 156 B); q4 (landcover) ran out of memory on all three endpoints ("Query run out of memory using about 2048 MB of RAM", nine tries) — the attic + relation line is too heavy for the public servers; not consumed before 4B-7, to be split (per landuse value, or the relation line alone) when it is. Raw JSONs live outside the repo in the job's scratch (`~/.claude/jobs/27dfa09b/osm/ring_2026-09-22T08-45-51Z/`), q1+q2+the .ql files+manifest copied to `/tmp/fd-4B2-osm/`.
- tools/world/skeleton.py — §3 and §4 as one deterministic script: pyproj 4326 -> 25832 always_xy, x = E - 352 000, z = -(N - 5 577 000), mm-rounded (-0.0 folded to 0.0); junctions where ≥ 2 q1 ways share a node, every way split there (segment id = way id + "-" + split index, junction id = node id); Douglas-Peucker 0.3 m on each piece with the plan's rule on top — a node whose heading changes by more than 1° is never removed (at OSM's ~19 m spacing a 1° bend sits 0.17 m off its chord, so plain DP would eat it: measured, plain DP 0.3 m would have removed 8 819 bends over 1° in this bbox); widths by tag or class, `width_source` "tag"|"class" recorded per segment; the carried tags (bridge, layer, maxspeed, name, oneway, ref, surface, tunnel) only where the OSM tag exists, as strings; relation 38566's members chained by shared endpoint nodes into `loops: [{id: "nordschleife", rel: 38566, segments: [...]}]`; segments and junctions sorted by id, keys sorted, separators compact, query_sha = sha256 over the six .ql files in q1..q6 order (68c80d14fc89ef4af05135f0a3e215fdf577264a9c45ac9ab7ac2f7c62a939bb). `--selftest` (run, PASSED): pyproj round trip 0.0000 m at the origin and at 20 km east/north/north-east; on the checked-in sample 10 of 136 nodes removed, worst 0.216 m off its chord, 120 of 120 bends > 1° kept; on the fixture 3 of 15 removed, 2 of 2 kept; on the snapshot's q1 ways split at junctions 8 466 of 139 468 removed (worst 0.2999 m), 96 729 of 96 729 bends kept; the sample's three ways in the output (Karussell 1 segment, Hohe Acht 3, Steilstrecke 2, endpoints within 0.052 m of the sample's 1e-6° lat/lon); the Karussell 7.5 m; the loop 92 segments of 52 ways, 20 782.7 m (within 1 % of 20 830; the doc's node-chord sum was 20 746, the split-and-DP'd chain 37 m longer because the junction nodes with the five service ways and the R18 access are kept); built twice, the same bytes. `--reference` prints the projection table the suite embeds. Determinism gates: (a) two runs on the same raws, cmp exit 0; (b) a fresh q1+q2 pull into a second folder (served timestamps 01:00:50Z/01:01:51Z vs 00:57:37Z — the raw JSONs differ only in that header line) -> skeleton.py -> cmp against the working-tree skeleton.json, exit 0.
- scripts/skeleton_loader.gd — SkeletonLoader, static as ElementCatalogue is, no autoload: read_file(), typed Segment/Junction/Loop records (points as PackedVector2Array of [x, z]), segments()/segment(id)/junctions()/junction(id)/loops()/loop(id)/snapshot()/origin() read once on the first lookup, segments_of()/junctions_of()/loops_of() for any parsed dictionary, validate(data) returning one fault line per wrong thing (schema, the pinned snapshot, the frame, every segment's id form and order and width rule and points, every junction with ≥ 2 segments each ending on it and every segment end on a junction listed by it, every loop chaining and closing within 0.5 m), the constants (width table, Karussell, band, carried tags, id patterns), and wgs84_to_local(lat, lon): the pipeline's projection done in GDScript (GRS80, UTM 32N, Krüger series to n^6) so the suite can hold the file to pyproj's numbers without pyproj — flagged "chosen for the skeleton"; nothing consumes the skeleton yet.
- data/regions/eifel_ring/skeleton.json — generated, in the working tree for the gates, NOT committed by the job (the size gate above).
- tests/skeleton_test.gd — the suite's new step after the element catalogue test, 170 ok lines: the file parsed twice to the same bytes and validate() silent; snapshot pinned to 2026-09-22T08:45:51Z (its own literal, cited to ring-region-decisions.md §1), bbox and origin the Ring's; 16 771 segment ids of the form and unique, ≥ 2 points each, sorted; 10 700 junctions with ≥ 2 segments, both directions of the junction↔segment consistency (31 396 ends); widths one line per class (13 classes present) by class the table's and by tag inside [0.5, 2] × class, no raceway by tag, the Karussell 7.5; the loop: relation 38566, 91 join lines each within 0.5 m (every join is 0.000 m: shared nodes), closes, 20 782.7 m over 92 segments of 52 ways within 1 % of 20 830, all raceway and oneway=yes; the projection reference (11 rows from `skeleton.py --reference`, pyproj 3.8.0 / PROJ 9.8.1): the loader's projection within 0.0000 m at the origin and ≤ 0.0010 m everywhere else (limits 0.01 / 0.5 m); the sample's three ways present with endpoints within 0.052 m; the lookups; the five-way fixture built in code (a crossing, a dead end, widths by class/tag/ignored, a two-way loop) through the same checks; validate() on 18 broken fixtures naming the thing and the field. No network, no python: the DP proofs live in --selftest.
- run_tests.sh: the one step (no --fixed-fps, static data) plus the header's enumeration and "fifteen tests"; tests/README.md's enumeration; README.md's Tests prose gains the step in house style. The step writes nothing under /tmp.

Decisions made on the pipeline's behalf (flagged "chosen for the skeleton" in the code; the driver may veto any): the loop anchor `loops` (the documented schema has none; the closure test needs one), chained from shared endpoint nodes with the start pinned to the member way with the smallest OSM id (41395647) and its smaller endpoint node — which runs WITH the oneway direction; `width_source` per segment; the plausibility band for a `width=*` tag [0.5, 2.0] × the class value (2 733 of 16 771 segments take their tag: 2 614 of them tracks; a tag outside the band, e.g. "40", falls back to the class); `*_link` classes at their parent's width and motorway/trunk at the 7.5 m carriageway (the 2.5 m hard shoulder is R13's own; no motorway in the bbox anyway); the heading-keep rule over DP; the attic-header check; q6's composition. pyproj is not in the system python: the job's venv (`python3 -m venv venv && venv/bin/pip install pyproj`, pyproj 3.8.0 on PROJ 9.8.1) lives in the job scratch, never installed system-wide; the suite needs neither. Decided by the Conductor: (1) the full 5.95 MB file checked in (determinism gate (b) proven; §9's own volume estimate; generated+git-ignored breaks suite-on-fresh-clone; no shrink and no scope cut); (2) the "2 181 ways" in ring-region-decisions.md §2 and data-pipeline.md §9 corrected with a "was ->" (likely the paved classes only, ~2 208 without track and service); (3) q4's Overpass OOM ("Query run out of memory using about 2048 MB", all three endpoints, nine tries) recorded as a known issue — to be split per landuse value, or the relation line alone, before 4B-7 consumes it.
