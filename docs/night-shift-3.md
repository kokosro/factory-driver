# FACTORY DRIVER — NIGHT SHIFT 3 LOG (2026-09-22)
# Honest record of the shift: what was done, what remains. Companion docs: docs/design/,
# docs/art-direction.md. The detailed user-facing report lives outside the repo (project scratch).

## VERIFIED & PUSHED (every iteration double-gated: external job + orchestrator + independent re-run; suite byte-identical; certs byte-locked; zero errors)

| It | What | Commits | HEAD after |
|----|------|---------|-----------|
| 3X | Thermal reset semantics: R keeps heat, cools by physics | b445d79 | b445d79 |
| 3W | Mass ledger: 27-row mass table summing to kerb 1300 exactly, 38/62 split | 9b0fb7b | 9b0fb7b |
| 3L | Wear & aging: six accumulators over existing physics, staircase effects | e294ebe | e294ebe |
| 3K | Licence ladder: L0 sitting (7 elements), skid pad, the gate, ranks | c5343c5 | c5343c5 |
| 3Y | R does not refuel: reset keeps fuel like heat/wear (user's 12:55 bug) | 2a4ce24 | 2a4ce24 |
| 4A | Garage/menu on Tab + THE STUDY (21 live lessons, input display) + car condition + licence panel + data location (FD_DATA_DIR, copy-only migration) + HUD bar legend; menu_test step | d48b0cf | d48b0cf |
| 3Z | Licence sitting memory: per-element progress persisted, resume at first failed element, theory never retaken once passed, practice runs change nothing; instructor's car manual->auto after hill start (user's verdicts) | fdf7464 | fdf7464 |
| 4A.1 | UI readability: every surface fits 1280x720; 23 overflow checks walk the UI (the standing visual-defect rule encoded) | 3ba3cea | 3ba3cea |
| 3AA | Caster return like a real car: zero at standstill, none in reverse, speed-gated rolling return; five drivers re-taught to countersteer; certs re-baselined honestly | 9db6638 | 9db6638 |
| 3AC | Airborne honesty: unsupported wheels carry nothing (phantom force removed), ballistic flight, no drive/grip/rolling/hill in the air, full-droop wheels, honest landing; airborne_test step | 1db32ba | 1db32ba |
| 3AB | Wear measured in kilometres: rated lives (clutch 175k, pads 50k, tyres 25.5k/17k, engine 140k km, sourced/labelled estimates) + driving style as metres-of-life on top; equivalences measured (launch ~474 m clutch, stop ~252 m pads, donut ~1.26 km rear, idle ~31 km/h-equiv) | c5ee9c3 | c5ee9c3 |
| 3AD | Landing honesty: premise corrected by measurement — the bug was large-attitude seat extrapolation (266 kN catapult -> tumbling -> springs-flat "cat landing"), NOT the tail hop; exact rigid-body seat geometry above 0.1 rad, the shell as contact (bounded stops + friction), deterministic topple, no guaranteed four-down; the flip (F) rights an overturned car at rest, orientation only | 74aaef5 | 74aaef5 |
| 3AE | Flip visual fix: the level draw now writes the whole wheel/body transform (was: x/z kept the overturned carried picture — side-rest buried the wheels in the body, roof-rest mirrored them); fixed at the draw site so flip, reset and every settle-then-render path are covered; drawn layout asserted to the bit against car.tscn | b0ce888 | e5441d4+ |
| 3AF | Rollover momentum: overturned shell friction capped at ROLL_FRICTION_COEFF 0.45 x weight (kinetic friction of a car on its roof/side) — the CoM decelerates at <= 0.45 g while the roll continues about the moving contact; the wall-pivot is gone (worst per-tick CoM drop 1.15 m/s -> 0.075 m/s; the roll travels 17.4 m, rests on its wheels 43 m on, deterministically); upright shell contact untouched, 3AD numbers held byte-for-byte | 41189fa | 7d642ac |

Certified times at shift end (byte-locked): SLALOM 28.65 (was 28.82 before 3AA's caster; moved honestly, cited) / SPIN_180 15.95 / SPIN_360 18.07 / STOP_BOX 8.72 / REVERSE_180 7.72. Suite: 1281 ok across 13 headless steps.

## THE DRIVER'S CATCHES (all found by playtesting, all recorded with telemetry where given)
- 12:55 R-refuel bug -> fixed in 3Y. 14:56 sitting forgets passed elements -> 3Z. 16:15 STUDY tab off screen + the standing visual-defect rule -> 4A.1.
- ~17:25 ramp jump "wheels and body fell apart, joints stretched" -> 3AC (phantom wheel force). 20:35 retest "wheels stick to their axis... every jump ends up on all 4 wheels, like a cat" -> 3AD (large-attitude seat catapult; shell contact; no guaranteed landing).
- 23:30 flip with no wheels (side-rest: wheels invisible; roof-rest: wheels reversed) -> 3AE.
- 23:50 handbrake rollover "rolled around its axis without letting the lateral force move the car... like the car hit a wall" -> 3AF (rollover must tumble along its momentum, not pivot on a pinned contact).

## IN FLIGHT AT SHIFT CLOSE (honest state)
- 3AE + 3AF (the two catches above): external job editing scripts/car.gd (uncommitted), gates pending — it pushes itself when green. Certs byte-locked throughout.
- Design synthesis draft (art canon x gameplay, the driver's brain dump folded in) filed at docs/design/design-synthesis-draft.md — DRAFT, awaiting the driver's verdict before 4B.

## SHIFT-CLOSE ADDENDUM (00:40, after the user went to bed): 3AE + 3AF LANDED
Both fixes verified (double gate + independent re-run: suite 1292 ok across 3 runs byte-identical modulo the documented pid-path artifact, zero errors, certs byte-locked 28.65/15.95/18.07/8.72/7.72, pushed, HEAD == origin/main == 7d642ac):
- The flip's picture (3AE): the level-draw branch now writes whole transforms — side-rest flip shows all four wheels at their own corners (was: buried in the body, "no tires"), roof-rest flip shows left wheels left and right wheels right (was: mirrored). Fixed at the draw site, so flip, reset and any settle-then-render path are covered at once; asserted to the bit against the scene's layout.
- The roll's momentum (3AF): overturned, the shell's friction is the car's kinetic friction on the road at most (ROLL_FRICTION_COEFF 0.45 — reconstruction practice 0.4-0.5 g), so the centre of mass keeps travelling (decelerated <= 0.45 g) while the roll goes on about a contact that moves with it. The user's exact handbrake maneuver measured: was 1.15 m/s taken off the CoM in one tick (the wall), now 0.075 m/s worst tick; the roll travels 17.4 m while over (physics floor 15.5 m), ends on its wheels 43 m on; the diagonal rollover now slides ~15 m further on its side before resting. Where a roll ends is still the numbers' call — honest.

## DESIGN CANON REGISTERED THIS SHIFT
- docs/art-direction.md — the visual canon (verbatim driver brief; Porsche Unleashed PC 2000 aesthetic; road-is-the-composition; asset priority 1-4).
- docs/design/user-thoughts-economy.org — the driver's brain dump (verbatim): troc economy (goods/services/vouchers), cat ecology (population, scare level, 9 lives = 9 guaranteed landings, cats never die on the Ring, social gas stations), teleport bridges, living traffic (drivers die without a car unless they have a cat; school rigor scales with deaths), home-made cars + registration numbers, OSM skeleton world first, environment-dependent sound.
- The Ring as proving ground: jobs -> materials -> workshop -> build -> test on the Nurburgring -> car.

## WHAT REMAINS (queue, honest)
1. 3AE/3AF gates + push (in flight).
2. Driver's verdict on the design synthesis (open questions: building types, first OSM chunk scope, proving-ground pass rules, car ladder, first teleport destination).
3. 4B-prep: OSM skeleton world (data pipeline researched: docs/nordschleife-data-sources.md — DGM-2 LiDAR + OSM, free-with-attribution) + the Green Hell brief carrying the art canon; gravity-on-slopes physics rides here.
4. Then per the synthesis sequence: cats -> troc economy -> Ring proving ground -> living traffic -> sound -> Blender car pipeline (home-made models; the boxy placeholder is the canon violation to fix).
- Backlog (user-verdict pending or parked): tyre compounds per-compound thermal character; the STUDY's coming-soon lessons (drifting, quick turn need slide-reading pilots; traffic rules need the 4C world); recorded-telemetry scrub playback; 4A garage catalogue-skin pass; OSM continental dream (Phase 4 back end).

## METHOD (unchanged, for the record)
Headless verification only (the driver may be driving); every iteration = orchestrator subagent driving an external claude_code job + double gates + independent re-run; tests additive-only, checks never weakened; every retune documented was-> reason; suite byte-identical twice + --parallel; push only after gates. User canon: "we're not looking for drama, we are looking for real physics simulation."

## 4B-PREP (2026-09-23): THE DESIGN/RESEARCH PACKAGE, LANDED
Docs-only iteration (no game code touched). External job wrote, orchestrator gated (suite 3 runs: twice sequential byte-identical modulo the documented fd-3K-licence pid-path line, once --parallel; 1292 ok each, zero errors, certs byte-locked 28.65/15.95/18.07/8.72/7.72), committed, pushed.
- docs/design/4b/element-library.md — the element list, ordered by the canon's asset priority (R road segments, T terrain, S sky/light, B typed buildings, E economy buildings with privileges, V vegetation, F furniture); every entry: canon quote, OSM/DEM data that instantiates it, what is fixed in stone vs varies per region, the 2000 look.
- docs/design/4b/data-pipeline.md — OSM + DGM1 -> element assembly: extract/project/skeleton/drape/dress/bake, snapshot pinning, exact attributions.
- docs/design/4b/ring-region-decisions.md — the first region put in stone: bbox 50.30-50.45N 6.80-7.10E, origin E0 352000 N0 5577000 (EPSG:25832), OSM snapshot 2026-09-22T08:45:51Z, live counts (2181 drivable ways; relation 38566 = 52 ways, 1119 nodes, 20.7 km), POI inventory (9 fuel, 9 repair, 1 dealer, 1 driving school, 4 castles, 12 grandstands, 11 roundabouts), decisions marked PUT IN STONE / DECIDED / DEFERRED.
- docs/design/4b/first-run-flow.md — pin -> test centre -> licence -> voucher -> rental -> first car, grounded in existing code (licence_manager/study/garage/data_dir/odometer_store cited by line), frozen licence semantics untouched.
- docs/design/4b/implementation-plan.md — 4B-1..4B-8 gated iterations (library -> skeleton -> drape -> drivable Ring -> buildings -> first-run UI -> dressing), each with files, additive tests, headless definition of done; GRAVITY-ON-SLOPES scoping (design-only): slope physics delivered through the existing RoadProfile injection seam (car.gd:2818 export, ramp_gradient 4677, sample_height 4693, the hill step 4130) so car.gd stays untouched and the flat-pad certs stay byte-locked (ramp_gradient is Vector2.ZERO on the flat pad).
- docs/design/4b/samples/ — OSM Karussell extract (3 ways of relation 38566, 4.2 KB) + LICENSE-NOTES.md with the exact OSM ODbL and DGM1 DL-DE BY 2.0 attributions, live-verified with URLs + access date.
Live data findings recorded: DGM1 (1 m, RLP) is now free open data — upgrade on the recorded DGM-2; DGM1 tiles direct HTTPS no account (geobasis-rlp.de, metalink-indexed, SHA-256); touristenfahrten.geojson declares NO licence (verification only, never shipped). Open questions for the driver sit at the end of each doc (road width 5 vs recorded 8-9 m; per-car licence vs the first car's entry; the social station and cat sanctuary picks; DL-DE vs CC BY-SA; DGM1 vs DOM1 for the Karussell bank).
## 4B-1 (2026-09-23): THE ELEMENT CATALOGUE AS DATA, LANDED
First 4B iteration built. External job wrote, orchestrator gated and pushed (commit 7fb5fab, HEAD == origin/main). Suite 1749 ok across five runs — the job's two sequential (byte-identical modulo the documented fd-3K-licence pid-path line at 1679, the only allowed artifact), the orchestrator's two sequential, one --parallel, plus the independent re-run on the git archive HEAD copy of the commit; zero SCRIPT ERROR/ERROR:/Parse Error everywhere; certs byte-locked 28.65/15.95/18.07/8.72/7.72; frozen files' diff empty (car.gd, road_profile.gd, test_pad.gd, asphalt_texture.gd, mission_manager.gd, handling_tests.gd, licence_manager.gd, scenes/, project.godot, configs/cars/, configs/validation.gd); tests additive-only (one new file, run_tests.sh's one step, the README rows); user-thoughts.org untouched.
- configs/elements/*.json — the library of element-library.md transcribed as data, one file per family, 81 entries: R 18 / T 9 / S 6 / B 11 / E 14 / V 8 / F 15. Every entry: id, family, title, priority (the canon's asset priority of its family), the canon line it cites, the OSM/DEM data, what is stone (plus stone_parameters: the numbers and names the library states), what a region table may vary (each parameter with a range or choices and its default inside), its texture budget, and the library's asides as notes.
- scripts/element_catalogue.gd — ElementCatalogue: a class of static functions as CarConfigValidation is, no autoload; read_files(), entry(id), by_family(letter), validate(files) returning one fault line per wrong thing (file, entry, field); the schema constants (id pattern, family priorities 1-9, the privilege vocabulary of the E-table, the canon's texture budgets, the R surface classes with gravel's friction "to be measured"), and nothing that touches the game — no mesh, no scene, no physics reads it yet (4B-5 and later do).
- tests/element_catalogue_test.gd — the suite's new step (after the config test, same family: cheap static data that fails first), 457 ok lines: every entry held to the plan's asks (id of the documented form and unique, canon cited, priority 1-9 and its family's, every varying parameter with its default inside its range or choices, texture inside the family budget, the E-table's shells real B ids and privileges from the vocabulary), the files parsed twice to the same bytes, the counts held to the library's (a dropped or invented entry fails here), entry()/by_family() answering, and validate() on 16 fixtures broken in code naming the entry and the field — never a file on disk broken.
- run_tests.sh's header and the --parallel wording updated (fourteen tests), tests/README.md's enumeration corrected (it had said "twelve" and omitted the airborne test: now says what runs), README.md's Tests prose gains the step's description in house style.

Decisions made on the library's behalf (flagged "chosen for the catalogue" in the JSON; the driver may veto any): where the library names a parameter without numbers, ranges were chosen and flagged (R9 bank 20-40 % bowl 6-12 m flat strip 0.5-2 m; R11 lamp spacing 10-40 m; R13 lanes 2-4; B0 roof pitch 25-55°; B10 gates 2-8; T2 treeline 0-3000 m; S1 sun elevation 0-90°; S6 rain intensity 0-1); defaults where the library gives a range but no default lean to the Eifel or the canon's first-named value (R1 lane width 3.5 m, R3 outside barrier F1, R10/F11 steel, R11/F12 concrete, R12 rock, T3 grey slate, T8 grey-green, B0 white, B5 grey); choice lists hold only what the library names (several single-choice); the S family has no line in the canon's texture budget so its budget is empty and unchecked; E2's social 10 % sell_tuna variant and E5's car storage are notes, not privileges (the vocabulary check covers each entry's base set); E14's shell is the string "any", its privileges the region table's.

## 4B-2 (2026-09-23): THE OSM EXTRACT AND THE RING SKELETON, LANDED
Second 4B iteration built and gated by the external job; the size gate went to the Conductor and the verdict was the full file checked in (the job's brief: a skeleton over 1 MB is the Conductor's decision, not the job's): the honest full-bbox skeleton is 5 953 503 bytes, sha256 b86984bac5ba2b0d686616228fc015f912cb1abf1c82bb5484454926668ef42f, because the bbox holds 9 188 drivable ways at the pinned snapshot, not the 2 181 the design package recorded (ring-region-decisions.md §2 and data-pipeline.md §9 say "2 181 ways"; the attic pull answers 9 188: track 5 444, service 1 536, residential 1 169, tertiary 338, primary 278, secondary 211, raceway 98, unclassified 87, primary_link 15, secondary_link 5, living_street 4, tertiary_link 3 — 2 208 without track and service, so the recorded count was likely of the paved classes; the "was ->" corrections and the open-question-4 decision are in those docs now). Sizes of the honest alternatives, same pipeline, same rounding: all q1 classes 5 953 503 B (16 771 segments, 10 700 junctions, 131 002 points); without track 2 059 781 B; without track and service 1 271 334 B; primary..unclassified + raceway + links 638 208 B; relation 38566 alone 35 731 B. Everything else is complete and gated with the full file in the working tree: suite 1 919 ok (1 749 + the new step's 170) across three runs — twice sequential byte-identical modulo the documented fd-3K-licence pid-path line (line 1853 this time), once --parallel; zero SCRIPT ERROR/ERROR:/Parse Error; certs byte-locked 28.65/15.95/18.07/8.72/7.72 (cmp against the 33-line cert base, exit 0 in all three); frozen files' diff empty; tests additive-only; user-thoughts.org untouched.
- tools/world/extract_osm.py — the six Overpass queries of data-pipeline.md §2.1 verbatim, each with the attic date `[date:"2026-09-22T08:45:51Z"]` in its settings block (q6 composed from the doc's sentence: barrier ways, highway=street_lamp|stop|give_way ways, nwr traffic_sign, power=pole|line ways, nwr railway=level_crossing|rail; flagged "chosen for the skeleton", nothing in 4B-2 reads it), written as .ql files into a snapshot folder named by §7 (`ring_2026-09-22T08-45-51Z`), fetched with curl exactly as §2.1's loop (form-encoded `data@`, UA `factory-driver-4b/0.1 (world pipeline; okaypeace@gmail.com)`, --max-time 300, 10 s apart, three tries per endpoint 30 s apart, lz4 then kumi then overpass-api.de), a manifest.json per snapshot (endpoint, counts by type, bytes, sha256, the server's own timestamp). Measured and recorded in the script: an attic answer's `osm3s.timestamp_osm_base` is the serving database's live time (00:57-01:01Z on 2026-09-23), NOT the attic date; the data is the attic view all the same (q2 answered the recorded 52 ways / 1 119 nodes exactly; a second pull 3 minutes later gave the same skeleton to the byte), so the check is "served at or after the attic date" and the snapshot's identity is the date in the .ql files. Raw pulls, this snapshot, all from lz4.overpass-api.de: q1 9 188 ways (10 892 275 B), q2 1 relation + 52 ways (91 686 B), q3 989 ways + 58 nodes + 2 relations (867 778 B), q5 17 635 ways (12 872 163 B), q6 600 ways + 44 nodes (510 156 B); q4 (landcover) ran out of memory on all three endpoints ("Query run out of memory using about 2048 MB of RAM", nine tries) — the attic + relation line is too heavy for the public servers; not consumed before 4B-7, to be split (per landuse value, or the relation line alone) when it is. Raw JSONs live outside the repo in the job's scratch (`~/.claude/jobs/27dfa09b/osm/ring_2026-09-22T08-45-51Z/`), q1+q2+the .ql files+manifest copied to `/tmp/fd-4B2-osm/`.
- tools/world/skeleton.py — §3 and §4 as one deterministic script: pyproj 4326 -> 25832 always_xy, x = E - 352 000, z = -(N - 5 577 000), mm-rounded (-0.0 folded to 0.0); junctions where ≥ 2 q1 ways share a node, every way split there (segment id = way id + "-" + split index, junction id = node id); Douglas-Peucker 0.3 m on each piece with the plan's rule on top — a node whose heading changes by more than 1° is never removed (at OSM's ~19 m spacing a 1° bend sits 0.17 m off its chord, so plain DP would eat it: measured, plain DP 0.3 m would have removed 8 819 bends over 1° in this bbox); widths by tag or class, `width_source` "tag"|"class" recorded per segment; the carried tags (bridge, layer, maxspeed, name, oneway, ref, surface, tunnel) only where the OSM tag exists, as strings; relation 38566's members chained by shared endpoint nodes into `loops: [{id: "nordschleife", rel: 38566, segments: [...]}]`; segments and junctions sorted by id, keys sorted, separators compact, query_sha = sha256 over the six .ql files in q1..q6 order (68c80d14fc89ef4af05135f0a3e215fdf577264a9c45ac9ab7ac2f7c62a939bb). `--selftest` (run, PASSED): pyproj round trip 0.0000 m at the origin and at 20 km east/north/north-east; on the checked-in sample 10 of 136 nodes removed, worst 0.216 m off its chord, 120 of 120 bends > 1° kept; on the fixture 3 of 15 removed, 2 of 2 kept; on the snapshot's q1 ways split at junctions 8 466 of 139 468 removed (worst 0.2999 m), 96 729 of 96 729 bends kept; the sample's three ways in the output (Karussell 1 segment, Hohe Acht 3, Steilstrecke 2, endpoints within 0.052 m of the sample's 1e-6° lat/lon); the Karussell 7.5 m; the loop 92 segments of 52 ways, 20 782.7 m (within 1 % of 20 830; the doc's node-chord sum was 20 746, the split-and-DP'd chain 37 m longer because the junction nodes with the five service ways and the R18 access are kept); built twice, the same bytes. `--reference` prints the projection table the suite embeds. Determinism gates: (a) two runs on the same raws, cmp exit 0; (b) a fresh q1+q2 pull into a second folder (served timestamps 01:00:50Z/01:01:51Z vs 00:57:37Z — the raw JSONs differ only in that header line) -> skeleton.py -> cmp against the working-tree skeleton.json, exit 0.
- scripts/skeleton_loader.gd — SkeletonLoader, static as ElementCatalogue is, no autoload: read_file(), typed Segment/Junction/Loop records (points as PackedVector2Array of [x, z]), segments()/segment(id)/junctions()/junction(id)/loops()/loop(id)/snapshot()/origin() read once on the first lookup, segments_of()/junctions_of()/loops_of() for any parsed dictionary, validate(data) returning one fault line per wrong thing (schema, the pinned snapshot, the frame, every segment's id form and order and width rule and points, every junction with ≥ 2 segments each ending on it and every segment end on a junction listed by it, every loop chaining and closing within 0.5 m), the constants (width table, Karussell, band, carried tags, id patterns), and wgs84_to_local(lat, lon): the pipeline's projection done in GDScript (GRS80, UTM 32N, Krüger series to n^6) so the suite can hold the file to pyproj's numbers without pyproj — flagged "chosen for the skeleton"; nothing consumes the skeleton yet.
- data/regions/eifel_ring/skeleton.json — generated, in the working tree for the gates, NOT committed by the job (the size gate above).
- tests/skeleton_test.gd — the suite's new step after the element catalogue test, 170 ok lines: the file parsed twice to the same bytes and validate() silent; snapshot pinned to 2026-09-22T08:45:51Z (its own literal, cited to ring-region-decisions.md §1), bbox and origin the Ring's; 16 771 segment ids of the form and unique, ≥ 2 points each, sorted; 10 700 junctions with ≥ 2 segments, both directions of the junction↔segment consistency (31 396 ends); widths one line per class (13 classes present) by class the table's and by tag inside [0.5, 2] × class, no raceway by tag, the Karussell 7.5; the loop: relation 38566, 91 join lines each within 0.5 m (every join is 0.000 m: shared nodes), closes, 20 782.7 m over 92 segments of 52 ways within 1 % of 20 830, all raceway and oneway=yes; the projection reference (11 rows from `skeleton.py --reference`, pyproj 3.8.0 / PROJ 9.8.1): the loader's projection within 0.0000 m at the origin and ≤ 0.0010 m everywhere else (limits 0.01 / 0.5 m); the sample's three ways present with endpoints within 0.052 m; the lookups; the five-way fixture built in code (a crossing, a dead end, widths by class/tag/ignored, a two-way loop) through the same checks; validate() on 18 broken fixtures naming the thing and the field. No network, no python: the DP proofs live in --selftest.
- run_tests.sh: the one step (no --fixed-fps, static data) plus the header's enumeration and "fifteen tests"; tests/README.md's enumeration; README.md's Tests prose gains the step in house style. The step writes nothing under /tmp.

- The codex read-only cross-review of the two Python tools (gpt-6-astra, read-only) found four real robustness faults in the fetch/validate paths and audited the checked-in artifact itself clean (topology, widths, junction membership and coordinates, canonical bytes, the loop's stored orientation: 0 mismatches), landed as a24db31: q6's lamps, stop/give_way and power poles read on nodes too (`nwr` — the sentence's own reading; the composition choice now cited to the review), an answer carrying an Overpass `remark` is refused even when it holds elements (a 200 answer with "runtime error: Query timed out" and partial data used to be accepted), a fetch is staged and committed by os.replace only once parsed clean (a failed retry on a reused snapshot folder could overwrite a verified raw file while its old manifest entry stayed), skeleton.py verifies the raw files' sha256 against the manifest when one is there, the loop chain is held to its stored points and the oneway direction (it walked node ids before), and skeleton.snapshot gains `pipeline_version: 1` (data-pipeline.md §7's version field; loader and test hold it). The artifact changed only by the version field and q6's new query_sha: 5 953 524 bytes, sha256 f5f2498f1517ec6ebeb6a3f8d48f3beafbebc1fbb6824f90dc0ce02237b2333d, query_sha 27dbf3ab50baea77d43f7363aca8e21fd246014ebfcf402f9d4f90abd88650c0 — was 5 953 503 / b86984ba… / 68c80d14…. Gates re-run after: suite twice sequential byte-identical (1 920 ok — the version line is the +1), --parallel, the git-archive-HEAD copy of a24db31 (1 920), certs byte-locked all runs, error grep 0, determinism (a) and (b) re-proven on the v2 artifact (cmp exit 0 both).
- Decisions made on the pipeline's behalf (flagged "chosen for the skeleton" in the code; the driver may veto any): the loop anchor `loops` (the documented schema has none; the closure test needs one), chained from shared endpoint nodes with the start pinned to the member way with the smallest OSM id (41395647) and its smaller endpoint node — which runs WITH the oneway direction; `width_source` per segment; the plausibility band for a `width=*` tag [0.5, 2.0] × the class value (2 733 of 16 771 segments take their tag: 2 614 of them tracks; a tag outside the band, e.g. "40", falls back to the class); `*_link` classes at their parent's width and motorway/trunk at the 7.5 m carriageway (the 2.5 m hard shoulder is R13's own; no motorway in the bbox anyway); the heading-keep rule over DP; the attic-header check; q6's composition. pyproj is not in the system python: the job's venv (`python3 -m venv venv && venv/bin/pip install pyproj`, pyproj 3.8.0 on PROJ 9.8.1) lives in the job scratch, never installed system-wide; the suite needs neither. Decided by the Conductor: (1) the full 5.95 MB file checked in (determinism gate (b) proven; §9's own volume estimate; generated+git-ignored breaks suite-on-fresh-clone; no shrink and no scope cut); (2) the "2 181 ways" in ring-region-decisions.md §2 and data-pipeline.md §9 corrected with a "was ->" (likely the paved classes only, ~2 208 without track and service); (3) q4's Overpass OOM ("Query run out of memory using about 2048 MB", all three endpoints, nine tries) recorded as a known issue — to be split per landuse value, or the relation line alone, before 4B-7 consumes it.

## 4B-3 (2026-09-23): THE ELEVATION DRAPE AND THE WORLD PROFILE, LANDED
Third 4B iteration built and gated by the external job. The DGM1 pull went exactly as data-pipeline.md §2.2 says (the three district metalinks 07131/07137/07233, published 2026-08-12, filtered to the core E 352-358 × N 5577-5582 km = 42 tiles, every tile year 2025, 68 294 749 bytes; four tiles at a time were throttled to six missing files, refetched one at a time; 42 of 42 sha256-verified against the metalinks before use; the tiles live in the job's scratch `~/.claude/jobs/a61f3c60/dgm1/`, the metalinks + urls.txt + SHA256SUMS + the two tile metadata XMLs copied to `/tmp/fd-4B3-dgm1/`; no .tif ever in the repo). The venv, never system-wide: `python3 -m venv venv && venv/bin/pip install rasterio pyproj` → rasterio 1.5.1 on GDAL 3.12.4, pyproj 3.8.0 on PROJ 9.8.1 (the toolchain pin for reproducibility; pyproj is the skeleton stage's, the drape needs rasterio + numpy). Suite 2 028 ok (1 920 + the new step's 108) across three runs — twice sequential byte-identical modulo the documented fd-3K-licence pid-path line (line 1971 this time; the first run after a new class_name also prints the import's one-time "update_scripts_classes | WorldRoadProfile" registration lines, the second and the --parallel run are warm), once --parallel; zero SCRIPT ERROR/ERROR:/Parse Error; certs byte-locked 28.65/15.95/18.07/8.72/7.72 (cmp against the 33-line cert base, exit 0 in all three); frozen files' diff empty (car.gd, road_profile.gd subclassed in a new file only); tests additive-only; user-thoughts.org untouched. The one q4 item of 4B-2 (the landcover query's Overpass OOM) is NOT touched here.
- tools/world/drape.py — data-pipeline.md §5 as one deterministic script (§7): the tiles merged by rasterio into one float64 grid in game metres (AREA_OR_POINT=Area checked: a value is its cell's centre, bilinear between centres, valid half a cell in from the edge; a nodata cell would refuse the run, the core has none; each tile's sha256 recomputed and held to the folder's SHA256SUMS, listed in the file as `dem.tiles[{name, sha256, verified}]`); per skeleton segment inside the mosaic the platform's centre height at 2 m stations (`dense`, chosen for the drape: the skeleton's Douglas-Peucker'd chords run to hundreds of metres, heights at the points alone would cut through hills) and at the skeleton's points (`heights`, the documented schema), a `crossfall` per point (the three-point circle at each interior point, e = 8 m / R capped at R2's 4 %, 6 % under R3's 30 m hairpin radius, rising to the outside of the bend; the 2 % crown on a straight fading as 1 − min(|e| / 0.02, 1) so the platform's shape is continuous — chosen for the drape), the bridge rule (deck linear between the abutment samples; bridge=* other than "no" takes it, OSM's one viaduct included — chosen for the drape) and the tunnel rule verbatim (|layer| × 6 m under the DEM, ramped in over 30 m from each portal), `labels` [{at, kind: crest|dip, curvature_20m, curvature_40m}] from the second difference over the 20 m and 40 m windows, one label per run beyond 0.004 /m at its steepest station; `covered` per segment (165 segments cross the core's edge: their heights are null outside, no labels, the profile treats them as terrain); the terrain lattice at 10 m INSIDE drape.json (chosen for the drape: 701 × 601 = 421 301 heights ≈ 3 MB; 5 m would be 12 MB; one file as the contract says); heights to the centimetre (chosen for the drape: the DGM1's stated accuracy is ± 10 cm + 5 % of the grid, a millimetre would be false precision and a third of the bytes), crossfall to 1e-4, curvature to 1e-5, chainage to the mm; rounding is floor(x × 10^n + 0.5) / 10^n in BOTH the writer and the GDScript reader (chosen for the drape: Python's round() is half-to-even on the exact binary value and 12 of 3 314 segments' 40 m curvatures came out a digit apart in the reader's recount before this); -0.0 folded; keys sorted, separators compact, no timestamp; `snapshot` carries the skeleton's osm_base/bbox/query_sha, its pipeline_version and its sha256, and the drape's own pipeline_version 1; `rules` records the constants so the reader refuses a file of other rules. CLI `--tiles DIR --skeleton PATH --out PATH`, `--report`, `--section WAY` (a way's cross-sections: the Karussell evidence), `--tile-prefix dom1`, `--selftest` (PASSED: a synthetic plane + bowl + crest, the plane back within 6e-14 m, the crest labelled at chainage 200 with its feet dips, a bridge linear, a tunnel 6 m under, the bend signs, a hairpin capped at 6 %, built twice the same bytes). Measured on the real run: the mosaic 7 000 × 6 000 cells, 304.88..680.65 m; 3 314 segments fully covered = 421.2 km of the skeleton's 3 277.6 km (12.9 %; the core is 30 km² of the bbox's ~350), 165 partly; the Nordschleife 20 782.7 m covered 100.00 % (its bbox x 48..6 167, z -5 972..-1 156 sits inside the core by 28 m at the north edge); 241 682 stations; 2 551 crests, 2 465 dips, 1 bank; 33 bridges and 10 tunnels draped. Built 2.9 s.
- The Karussell decision tree (ring-region-decisions.md §3, now with its "was ->"): the tile the docs name, 355_5580, does NOT hold the Karussell — the skeleton's first point of way 414785755 is E 356 780.6 / N 5 581 961.7, tile 356_5581 (1 km north-east; inside the core all the same; data-pipeline.md §2.2/§5 still say 355_5580 — not edited, out of this iteration's allowed list, for the Conductor's docs pass). (a) DGM1 356_5581: `--section 414785755`, 31 cross-sections every 5 m, ±8 m to the right of travel: the bowl is there but smeared by the 1 m gridding — the steepest 1 m cell 35.5 % over a 3-4 m band, the slope across the 7.5 m paved platform at most 19.5 % (18.2 % mean over the bowl's middle): under the tree's ≥ 20 %, (a) fails by the letter. (b) DOM1: the guessed path verified live — geoshop.rlp.de/files/anpassungen/hvd/products/dom1.json (200) names the per-district metalink `dom1_tif_07{kreissch}.meta4` and the tile pattern `dom1_32_<E>_<N>_1_rp_<year>.tif` (domb.json answers 404); dom1_tif_07131.meta4 fetched, dom1_32_356_5581_1_rp_2025.tif (2 398 554 bytes) pulled and sha-verified; the identical bowl, 19.6 % platform max / 18.2 % mean (the surface model differs from the ground model only outside the paved width: the fence and trees at +6..+8 m stand +3.4..+10 m): (b) fails the same way. (c) TAKEN: the R9 element's parametric bank, 30 % over a 6.5 m bowl with the 1 m asphalt strip at the inside (a left-hander: the bank rises to the right of travel), the centre anchored on the DGM1's centre height; the DGM1 corroborates the numbers: the inside-to-outside drop across the bowl measures 1.9-2.0 m and 30 % × 6.5 m = 1.95 m. drape.json's label: kind bank, bank 0.3, bowl_m 6.5, strip_m 1.0, at 0 to 152.915; the crossfall +0.30 at the way's 29 points.
- scripts/world_road_profile.gd — WorldRoadProfile extends RoadProfile, a NEW file (road_profile.gd frozen, untouched): the four calls the car and the pad make answered with the world — sample_height = elevation_height (the pad's micro-bumps, test dip, licence ramp and swells zeroed at construction): on a road the platform (the nearest covered chord's centre height at the stations, linear between them, plus the crossfall's shape across, or the bank's bowl), within the 6 m band outside the paved edge the edge eased into the terrain by smoothstep, off-road the lattice bilinear, exactly 0 outside the coverage; ramp_gradient by central differences over 1 m (Vector2.ZERO where any tap is outside: no cliff at the edge); elevation_mask 1 inside / 0 outside. Static, the shape of SkeletonLoader (chosen for the drape: one file as the plan lists, not a loader and a profile): read_file(), validate(drape, skeleton) with one fault line per wrong thing (the schema, the pins, the skeleton it was draped on, every tile verified, the coverage and the lattice against it, the rules against the reader's own, every segment in the skeleton with heights/crossfall aligned with its points and the dense list counted from its length, covered meaning no missing height, labels of the documented kinds inside the segment, a bank whose strip + bowl is the paved width, a crest's curvature negative and beyond the threshold), from_data(skeleton, drape) building an instance from parsed dictionaries (so the test hands it a fixture), ring() for the checked-in files; the rules mirrored operation for operation on 64-bit floats (station_chainages, chainages from the file's own numbers — SkeletonLoader's PackedVector2Array is single precision and a chainage a millimetre off would change a station count —, signed_curvature, superelevation, crossfall_of, tunnel_depth, labels_of, drape_segment(segment, points, sampler)). The one index, a 20 m cell grid over the covered chords (each filed under every cell its box grown by 10.25 m — the raceway's half width plus the band — overlaps; a sample looks in its own cell), is built once in from_data, never lazily: 3 314 roads, ~18 000 chords, 295 ms to build, 103 ms to parse the 5.5 MB, 231 ms to validate, 100 000 samples in 143 ms (1.4 µs each). Purity as road_profile.gd's header: nothing in the sampling path keeps state. Known limit, documented in the header and held by the test: the field is single-valued — where a bridge crosses a road at another layer (T13's 41395673-0 over 421642912-0, 3.2 m below) the nearest centreline's height is answered and the field steps at the crossing; 4B-4 builds decks as their own meshes.
- data/regions/eifel_ring/drape.json — checked in, pre-approved: 5 480 097 bytes, sha256 b8d4e53117790b95581a11641e57d0693118efd9b93000f2314048f1c3672940 (under the 10 MB stop; the lattice is 3 MB of it). Determinism (a) drape.py twice on the same tiles: cmp exit 0; (b) re-derived from the verified tiles against the checked-in file: cmp exit 0.
- THE REFERENCE HEIGHTS (a STOP-AND-ASK, answered by the Conductor's "measure first, then decide" verdict): the loop's honest extremes came out 332.94 m lowest and 627.52 m highest against the recorded ~320 / ~620 ± 5. Measured on the verified tiles: the lowest dense sample is the shared endpoint of loop segments 41395647-0 (bridge=yes, 17.8 m) and 683006908-0 ("Breidscheid", untagged) at (2247.813, -5557.306); the raw DGM1 ground (no deck rule, no platform) at that point 332.94 m, on the track line within 100 m of it 331.83-342.98 m over 114 stations, the lowest ground of any kind within 150 m (the stream bed) 327.08 m — the ~320 was the recorded source's loose round, not a drape fault; the Hohe Acht way 414785756 tops at 616.50 m against the officially cited 616.8 m (0.3 m), the Nürburg castle hill reads 680.65 against the recorded 678: the tiles and the DHHN2016 datum are right. Re-pinned per the verdict, "was ->" in ring-region-decisions.md §3, docs/nordschleife-data-sources.md §1 and implementation-plan.md §4B-3's test line: Breidscheid was ~320 ± 5 -> 333 ± 2 m; the loop's top was "Hohe Acht / T13 ~620 ± 5" -> 627.5 ± 2 m at T13 (the Sabine-Schmitz-Kurve; 1009142894-0 chainage 10), the Hohe Acht way itself 616.8 ± 2 m. No tolerance widened: the bands are tighter, the values measured.
- KNOWN ISSUE for the Conductor (recorded, not fixed, reported by the test): the DGM1 is a ground model with bridges removed, and its hole reaches some metres past the OSM bridge ways' ends, so the untagged approach segments dip into the valley floor: 19 loop stations, all within ~10 m of a bridge-tagged segment's end (Breidscheid both sides: 683061814-1 chainage 98-104 falls 338.3 → 334.2, 683006908-0 chainage 0-8 climbs 332.9 → 337.3; T13 / Sabine-Schmitz-Kurve, Döttinger Höhe, Hohenrain, Galgenkopf), station-to-station slopes 50-64 %. The test measures the loop's steepest gradient away from those abutments (30.4 %, the Karussell's own bank across, at 414785755-0 chainage 30 — past MAX_SLOPE 1.5 %, under 35 %) and reports the abutment maximum apart (64.4 %, held under 70 %). A candidate rule (not taken: geometry the Conductor did not approve): extend a bridge's deck line over the untagged approaches until the ground meets it.
- tests/world_profile_test.gd — the suite's new step after the skeleton test (no --fixed-fps: static data), 108 ok lines: the file parsed twice to the same bytes and validate() silent, the seven top keys; the snapshot pinned, the drape's skeleton_sha256 equal to FileAccess.get_sha256 of the checked-in skeleton; 42 tiles, 42 verified; the coverage the core, the lattice 701 × 601 at 10 m with absolute heights; 3 479 segments (3 314 covered, 165 partly), every one of the loop's 92 covered, 241 682 stations; the labels recounted from the file's dense heights equal to the file's on all 3 314 (2 551 crests, 2 465 dips); 33 bridge decks linear between their abutments, one line each; the Karussell's crossfall and bank label, and the bowl read across at chainage 70 (inside edge 557.72, strip end 557.73 flat, centre 558.55 the DEM's, outside edge 559.68: 1.95 m); Breidscheid 332.94 within 333 ± 2, T13 627.52 within 627.5 ± 2, the Hohe Acht way 616.50 within 616.8 ± 2, sample_height on those centreline points returning the stations, the loop climbing 294.6 m; the slopes as above; 200 samples twice bit for bit; the fallback outside the core (west, east, north) 0 / (0, 0) / 0, half a metre inside the edge the world's height with a zero gradient, off every road the lattice's terrain; ring() the same profile, the pad's layers zero. Then the 3 × 3 km fixture built in code — a plane 400 + 0.05 x − 0.02 z with a 20 m bowl and two 3 m crests, six roads through the mirrored drape rules (a straight over a crest, a bridge over the other crest, a tunnel, a left-hand bend, way 414785755 as a left-hander, a diagonal across the lattice's seams), stations landing on centimetre-exact plane values: the drape passes validate(); the plane within 1 mm off-road (worst 6e-14) and on the centreline; the gradient the plane's within 1e-4 off-road and, on the centreline, (0, −0.02): the crown cancels across; the bowl 20 m at its centre node and 15 m 100 m out, flat at the floor; seams: steps 0 across (1000, −1000) and x = 2000 off-road and on the diagonal road; the 2 % crown 4 cm down at 2 m and 7 cm at the 3.5 m edge, the blend band halfway at 3 m and the terrain at 6 m; the bend superelevated 4 % (R 70.7 m capped), 16 cm across 4 m; the bridge's deck linear 458..468 m, 463 m over the 3 m crest under it (the ground 466), no crest labelled; the tunnel 454 at its portal, 451.75 at 15 m in, 453 under the 459 ground at mid-length; the crest labelled within a station of chainage 200 with its 20 m curvature beyond −0.004; the bank's bowl flat over the strip and 1.95 m up; the fallback outside the box; the stations, the superelevation table, the tunnel depths; and validate() on 18 fixtures broken in code naming the thing and the field. No network, no python, no DGM1; nothing written under /tmp.
- run_tests.sh: the one step plus the header's enumeration and "sixteen tests"; tests/README.md's enumeration; README.md's Tests prose gains the step in house style and a "Data sources & licences" note (§8's two exact strings).
- Decisions made on the drape's behalf (flagged "chosen for the drape" in the code; the driver may veto any): the 2 m dense stations beside the point-aligned heights; the 10 m lattice inside drape.json; cm heights; the crown-fade shape; e = 8 / R with the two caps for every class; bridge=viaduct as a bridge; the tunnel rule read as "0 at the portal, |layer| × 6 m from 30 m in" (§5's sentence admits the other reading, a deck-like line between portals would be the better road — an open question for the Conductor; 10 tunnels in the core, none on the loop); the floor(x + 0.5) rounding; one file for the loader and the profile; the cell index built at load; partly covered segments as terrain; the reference points derived from the file (the lowest and highest dense samples of the loop) since the docs record values, not coordinates.

- The codex read-only cross-review of drape.py + world_road_profile.gd (gpt-6-astra) audited the checked-in artifact CLEAN (all 3 479 records' alignment, crossfall and label re-derivation, cm rounding, lattice dims, the skeleton hash, canonical bytes; a full in-memory rebuild from the 42 tiles byte-identical; the test re-run headless green; road_profile.gd and car.gd diffs empty) and found four things; three fixed forward as 80b3917, one left as the recorded known issue: (1) the profile's fixed reach of 10.25 m (the raceway's 4.25 + the 6 m band) cut off the blend band of any road wider than 8.5 m — the skeleton admits a tagged width up to twice the class's — while its platform still contributed, a step in the field: now each chord is filed under the cells its box grown by its own road's half width + 6 m overlaps and counts only within that reach; the fixture gains a 14 m road by tag (its band halfway at 10 m, the terrain at 13 m, no step across the old cutoff: 2.7e-5 m) and the checked-in file's 200-sample readings are pinned by sha256 b167c232… (no covered segment is wider than 8.5 m, so the fix moves nothing there, and the test says so); (2) the mirrored draper's station interpolation passed through a single-precision Vector2 before sampling: on 1017207289-0 chainage 4 over the plane 400 + 0.05 x − 0.02 z the station's true height is 698.02499711 m, a centimetre boundary, and the float32 detour rounded it 698.03 against the pipeline's 698.02 — the helper now returns [x, z] as 64-bit floats and the test holds the reader to a 64-bit interpolation done there and to the pipeline's value; (3) drape.py merged the tiles with rasterio.merge, which fills cells no tile covers with ZERO when the rasters carry no nodata (fictitious valid terrain the nodata-is-None check never saw) and a NaN nodata escaped the equality check — the mosaic is now assembled by the script itself into a NaN-filled rectangle spanned by the tile names, each tile's own nodata (NaN included) mapped to NaN, and any cell without a finite height refuses the run; --selftest gains the four-tile placement (z grows southward: −0.5 is the south edge), the corner blend and the three refusals (a missing tile: 1 000 000 cells; a NaN; an infinity). drape.json rebuilt from the verified tiles after the change: byte-identical (cmp exit 0), the real core has no hole. (4) NOT fixed, the Conductor's call, already recorded above: bridge ends are taken as abutments and the tag overrides contradictory ground unconditionally — the review's concrete instance, bridge 41226730-0's deck 1.472 m BELOW the DEM at chainage 94 (deck 609.71, ground 611.18), joins the abutment-hole evidence; the candidate approach-extension rule remains not approved. Gates after: suite 2 032 ok (2 028 + the 4 new lines) — twice sequential byte-identical modulo the pid line (the first run after a script change also prints the import's class re-registration lines, the second is warm), --parallel, the git-archive copy of the final HEAD, certs byte-locked (cmp exit 0 in every run), error grep 0, determinism (a) and (b) re-proven with drape.json unchanged.

## 4B-4 (2026-09-23): THE NORDSCHLEIFE AS A DRIVABLE ROAD, LANDED
Fourth 4B iteration built and gated by the external job (a predecessor job had left two uncommitted drafts, scripts/road_builder.gd and scenes/eifel_ring.tscn, and died mid-measurement; audited, the sweep's shape and the collision decision kept, its recursive mesh bisection replaced - it asked the profile millions of times and its headless build ran past five minutes -, its rim rule brought to the ruling's 20 % and a multi-segment walk, the scene's Car put before the Road so the car settles once). Everything headless; the pad, main.tscn, car.gd, road_profile.gd, world_road_profile.gd and every cert untouched (the frozen list's diff against ac80256 empty; drape.json sha256 b8d4e531... byte-identical, pinned by the new test).
- scripts/road_builder.gd (NEW, RoadBuilder extends Node3D) - every drape-covered skeleton segment swept into a strip mesh with a trimesh collider, built in _ready from the checked-in files: no bake step, no editor. THE SURFACE IS THE FIELD: every vertex's height is asked of the WorldRoadProfile at the vertex's own stored (single-precision) point, so the mesh carries the platform, the bank and the profile's single-valued steps exactly as the car feels them; cross-sections at every breakpoint of the field along the road (the 2 m stations, every skeleton point, the crossfall's crossings of 0 and ±2 % where the crown's share kinks) so the mesh is exactly linear where the field is, and each interval split by the analytic twist bound (Δb × half width / 4 under 1 mm, computed from the crossfall, not probed); mitred sections at kinks (the bisector stretched by 1 / cos of half the kink, capped at 2 ×); the geometry from the files' 64-bit points chained by WorldRoadProfile.chainages, never through SkeletonLoader.Segment.points. Elements: R17 the circuit strip (edge, crown line, edge), R9 the banked cross-section at the Karussell (edge, the strip's end where the bowl begins, edge - the bank rising to the side the crossfall says), R18 the pit lane and access links as plain strips. Built: 3 304 roads (the 3 314 covered less the ten crossing structures below), 329 076 sections (93 020 added by the twist bound), 987 228 vertices, 1 303 088 triangles, 3 304 StaticBody3D trimeshes and one follower floor, 7.9-9.9 s wall on a machine at load average 7 (about a quarter of a core; the profile's sample costs ~5 µs in GDScript, ~1 M samples per build).
- chosen for the ring build (flagged in the code): the mesh carries the paved platform only - the 6 m blend band is terrain, the lattice ground a later dressing pass (4B-7), no verges faked; vertex heights from the profile, never a parallel copy of its rules; one MeshInstance3D + one StaticBody3D per road; THE COLLISION SPLIT (Conductor-approved 2026-09-23): the trimeshes sit on collision layer 2 with an empty mask, a layer the car's mask (1) does not include - the CharacterBody3D stays upright (car.gd:87-95: pitch and roll are the body's on it) and its level box, GROUND_CLEARANCE 0.12 m over the road, would be shoved by a sloped trimesh on any grade past ~5 % (0.12 m over the 2.1 m to the box's end; a 20 % descent puts the box's rear corner 0.30 m under the road behind); the car stands on the profile as the plan's seam says and meets the road as it meets the pad: the pad's proven level follower (test_pad.gd's _follow_car) carried onto sloped chords, one floor put under the car every tick at the SAME corrected profile's height the mesh and the car read (no third height source; the test holds the floor to the field every tick of the 2 km drive, worst 0.03 mm, and its tick-to-tick step to the field's own over the car's way, worst 0.018 m). FAILURE MODE FOUND AND FIXED: the physics server takes a moved static body's place at its next step, so the follower is where the car WAS for one tick; with the pad's endless WorldBoundaryShape3D a car reset onto lower ground (R after a climb, the test's stands) sat inside the half-space for that tick and was ejected up to the old height - measured 69 m at the Karussell (that was the first run's "airborne on the Karussell, 8.5 m over the road" reading: a follower artifact, not the physics model; retired). Fix: Road.process_physics_priority = -1 (the floor placed before the car's step each tick) and the floor a 40 m × 0.1 m slab whose top is at the road, not a half-space: a slab left behind on the old ground is not under the car at all.
- THE RIM RULE for the bridge approaches, applied to the PARSED drape before WorldRoadProfile.from_data (RoadBuilder.apply_rim_rule, a pure static function; the car's profile and the mesh carry one corrected field; drape.json untouched). The Conductor's original ruling worded the fix as "extend the deck where its linear extension sits > 1 m above the approach ground" - MEASURED FALSE at every loop bridge: the deck's abutment samples ARE the DGM1 hole's floor and the approach V drops to them (Breidscheid: the approach falls 338.76 → 334.20 over its last 8 m at 62-65 %, the deck 334.20 → 332.94, the next approach climbs 332.94 → 337.00 over 8 m at 50 %), so the deck's extension lies below the ground everywhere; what the same ruling requires - the 64.4 % spike must resolve - stands, and the rim rule honours it: Conductor-endorsed 2026-09-23, amendment 1 of the bridge fix ("my ruling's literal mechanism had a false premise - I presumed deck-above-hole; the loop's abutments ARE hole floors, the V drops to them - while the same ruling's explicit requirement 'the 64.4 % slope spike must resolve' stands. The rim rule honors it honestly: straight deck between the first 20 % rims, lift-only, 50 m cap, 2-segment junction walk, pure function of the parsed data, applied before WorldRoadProfile.from_data so mesh and car drive one corrected field, drape.json byte-identical, 41226730-0 deliberately untouched with its below-ground instance recorded as the reading the rule does not take"). The rule: from each end of a covered bridge walk outward along untagged covered approaches, at most 50 m; the RIM is the first outward station whose onward station-to-station slope is under RIM_SLOPE 20 % (the hole's walls climb 50-65 %/m, the loop's honest grades top at 15.8 %); the deck drawn straight between the two rims; every station of the bridge and of the approaches between the rims below that line lifted onto it, never lowered (the approach's own station at the junction lifted with the bridge's abutment sample: the same ground point); an abutment already climbing under 20 % is its own rim; an end without a rim in reach keeps its abutment. AMENDMENT 2, Conductor-endorsed 2026-09-23: with two-segment junctions only, Hohenrain's 41395668-0 east end kept its wall (its approach 41395670-0 is 4.1 m long and ends at the four-way T13 junction 65385912 still climbing at 54-60 %: 59.2 % before and after) - "leaving a ~60 % wall on the loop is a data artifact the canon forbids the car to feel ('not looking for drama... real physics'); the walk's 2-segment-junction limit was a simplicity choice, not physics"; the walk may now continue through a junction of three or more along the outgoing covered untagged segment that best continues the incoming heading (a turn of 60° at most; CONTINUATION_MAX_TURN_DEG), same rim, same 50 m, lift-only. Measured, before → after through ramp_gradient on the loop's own field within 14 m of each abutment: Breidscheid 41395647-0 west 64.4 % → 6.5 %, east 50.9 % → 6.5 % (rims 8.44 m before at 338.76 m and 8.00 m after at 337.00 m, the deck line -5.14 %, 19 stations lifted by up to 4.47 m; the brief's -4.8 % estimate took the rim at 10 m, the first station climbing under 20 % onward is at 8.44 m); 41395652-0 (Döttinger Höhe) west 32.0 % → 4.9 %, east 61.5 % → 4.5 %; 41395668-0 (Hohenrain) west 52.4 % → 7.9 %, east 59.3 % → 3.0 % via the continuation - it followed the straighter branch, the Boxengasse 769107218-0, not the loop's own 41395670-1: the rim 6.13 m out at 619.12 m, 41395670-0's 4.1 m and the Boxengasse's first 2 m lifted, the loop's spike at 41395670-1 chainage 10 gone (the amendment's "wrong shallow-angle branch" case, bounded as it says); 41395673-0 (T13) west its own rim 14.5 % → 5.0 %, east 53.6 % → 4.0 %; 41395681-0 1.3 % / 5.7 %, already clean, untouched. No station on the loop's own field reads a grade along the road of 20 % or more after the rule (was 19 at 50-64 %); the steepest honest grade 15.8 % on the Hohe Acht way 414785756-0 chainage 726. 16 of the 33 covered bridges lifted, 44 records replaced. 41226730-0 (the codex review's below-ground deck) UNCHANGED: its one walkable approach climbs away at 1-2 % so its abutment is its own rim, its other end stands at a junction the walk found no continuation through; its deck 1.21 m below the 10 m terrain lattice at chainage 94 (the review read 1.472 m against the 1 m DEM) - the reading the rule does not take, recorded. Every deck a straight line in the mesh (a lifted deck's stations exactly on the line; an untouched deck within the file's centimetre rounding, worst 6.7 mm).
- THE LOOP'S RIGHT OF WAY at layer crossings (RoadBuilder.apply_right_of_way, a second pure static transform on the parsed drape after the rim rule; chosen for the ring build, confirmed by the orchestrator 2026-09-23 under the endorsement pattern - a measured obstacle, the alternative in a frozen file, a goal-honouring transform -, the Conductor may veto with a trivial revert). Found by the first drive: the field is single-valued (4B-3's recorded known issue), so where a road passes under or over the loop the nearest centreline answers and the field steps inside the loop's paved width; the 2 km test drive hit a +2.88 m wall at 1 901 m from Döttinger Höhe (the track bridge 29898554-0 over Antoniusbuche) at 18 m/s and stopped dead. Measured on the loop: ten crossings, steps of 1.6-6.2 m against the corrected loop - along the lap from Döttinger Höhe: 890 m the tunnel 377340334-0 under the straight (-2.76 m), 1 903 m the track bridge 29898554-0 (+6.17), 2 827 m Hauptstraße 41455756-6 under Hohenrain (-5.71), 3 061 m 421642912-0 under T13 (-6.07), 4 799 m the primary 828126276-0 under 41395681-0 (-5.95), 6 933 m the track bridge 41795617-0 (+5.89), 10 342 m the track tunnel 41454323-0 (-2.10), 10 901 m Trierer Straße 828129905-0 under Breidscheid (-5.95), 12 930 m the track tunnel 288075148-0 (-1.61), 20 769 m the secondary 1050475919-0 under the Döttinger Höhe bridge (-6.21). The car reads sample_height(x, z) alone (car.gd frozen) and the profile has no hint which road it is on (world_road_profile.gd frozen): no pure rule serves both roads at a crossing; the loop is the deliverable. The transform: a covered segment off the loop (skeleton.json's loops entry nordschleife, 92 segments), sharing no junction with it, on another OSM layer than the loop segment, whose centreline passes within CROSSING_DISTANCE_M 1.0 m of a loop station (a centreline that crosses the loop's passes within half a station of one; a road beside the loop does not) at a centre height more than CROSSING_HEIGHT_M 0.5 m off the loop's (true layer crossings measured 1.6-6.2 m; a grade join - the Döttinger Höhe access, the pit lane - shares the loop's height at its junction and sits under it) is marked covered: false. Each test alone over-matched (the pit lane beside T13 lies on another layer 8 m away; the GP links join the T13 junction on another layer at the hole's floor) - the three together select exactly the ten. Deterministic (the drape's order, junctions by id), drape.json untouched. KNOWN ISSUE, recorded: the ten crossing structures - two track bridges, three tunnels, two primaries, one secondary, two tertiaries, 18-94 m each, 452 m in all - are not built this iteration and await a proper underpass/deck element in the dressing iterations; until then a side road simply reaches its junction with the loop. A crossing between two roads OFF the loop stays as the single-valued field has it (the same known issue, out of scope, not silently fixed). The whole loop swept along its centreline every metre (20 830 samples): no step over 0.5 m remains anywhere, the largest 0.413 m at 41395670-1 chainage 0 (the T13 junction, where the DGM hole's floor meets the junction roads).
- scenes/eifel_ring.tscn (NEW): WorldEnvironment, Sun, the Car (car.tscn, before the Road so it settles once - the Road's _ready hands it the corrected profile through road_profile's setter; a WorldRoadProfile cannot be a sub_resource in the file: from_data is a static constructor on parsed data and the corrected field exists only after the transforms), the Road (RoadBuilder, car path), ChaseCamera, HUD, Garage (car and hud only: no missions, licence or study in the Ring yet, the garage guards each). The car spawns at the PIT ANCHOR (ruling 2: certification sessions start at the pit area; ring-region-decisions.md §4's new line, chosen for the ring build): the skeleton's pit lane "Boxengasse an T13" 199642470-0 at chainage 20.0 m, 28.2 m before its junction with the Sabine-Schmitz-Kurve, x 2037.199 / z -1355.401, road height 627.44 m, yaw 33.19° heading along the pit lane toward the loop (the pit lane's first segment 199642469-0 is a bridge over the DGM's hole, its approach lifted by the rim rule up to 4.34 m; chainage 20 stands on honest ground past the rim).
- scripts/garage.gd - exactly three things: Garage.MAPS gains {"id": "eifel_ring", "title": "Nordschleife (bare road)", "scene": "res://scenes/eifel_ring.tscn"} appended after the pad (each map carries its own honest hint: the pad's "R puts the car back on the start line", the Ring's "the Ring's road alone, no dressing yet; the car starts at the pit area by T13 and R puts it back there"); _free_drive: was a push_error "changing scenes is not built yet" -> close() and get_tree().change_scene_to_file(map.scene) unless the map is the scene the garage is in (its parent's scene_file_path) - the car is re-instanced FRESH in the new scene at that scene's spawn (ruling 2; per-car continuity stays with queued item 3S: the store keeps the odometer, fuel, wear and the rest, where it was parked does not travel); the SETTINGS page gains "Road data:  © OpenStreetMap contributors — data licensed under ODbL 1.0, https://www.openstreetmap.org/copyright" (data-pipeline.md §8's exact string; the brief cited element-library.md §8 for it, which holds no such string) under the data location.
- tests/ring_drive_test.gd (NEW, the suite's step after the world profile test, --fixed-fps 60, 58 ok lines): drape.json's sha256 pinned; Garage.MAPS' Ring entry; apply_rim_rule and apply_right_of_way pure (twice on the parsed files, the same lifts, crossings and heights, the input untouched); the scene loads headless (the build's numbers reported), every loop segment stripped, a strip's layer 2 / mask 0 trimesh, the follower slab on layer 1, the car at the pit anchor on the road (0.002 m over it); THE MESH IS THE FIELD: all 62 259 vertices of the 92 loop strips and the 3 pit/access strips on the CORRECTED profile's sample_height within 1 mm (worst 0.031 mm); between the vertices the loop's quads probed at the along-edges' midpoints and the centres - 10 017 interior quads within 1.07 mm (the twist bound 1 mm plus single precision), 9 401 quads within the half width of a skeleton kink or a segment's end up to 196.9 mm off (the field's own step between two chords: their perpendicular offsets tie on the bisector and the nearest-chord answer changes chainage - a continuous mesh cannot follow a discontinuous field; reported, not asserted tighter), 925 quads with a vertex or probe on another road's field at junctions up to 2.86 m (the single-valued field's step at a junction, the same the car feels); the rim rule's numbers above, one line per bridge end with before/after and resolved: true (no residual left after amendment 2), the decks linear in the mesh, 41226730-0 unchanged; the ten crossings one line each (id, class and kind, length, layers, along-lap distance, the two heights, the step, removed: true), exactly the measured ten, the whole-loop sweep; THE DRIVE: a pure-pursuit follower of the loop's centreline (the 64-bit polyline from Döttinger Höhe onward; the lookahead 1.2 s of speed between 8 and 40 m; the cruise 18 m/s eased for the bends by a 4 m/s² lateral budget read off the heading change over the next 60 m; proportional pedals) fed through set_driver_input every tick - 2 000 m from 683303211-0 chainage 30 along the loop's one-way direction in 118.0 s (7 078 ticks), no wheel off the paved width: the worst lateral offset of the car's centre +0.258 m at 2 026 m along (the outer wheel 1.118 m from the centreline on a 4.25 m half width), speeds 17.0-17.5 m/s (mean 17.2, 62 km/h) after the pull-away, 0 airborne ticks, pitch up to 3.68° and roll up to 1.46° on the way, the two crossings passed (890 m and 1 903 m) on the loop's own field with every wheel carried, the follower floor on the field every tick; THE HILL STEP made executable: at the loop's steepest honest sample (414785756-0 chainage 726, 15.8 % along the road) WorldRoadProfile.ramp_gradient is (-0.0420, -0.1528), past the pad's MAX_SLOPE 1.5 %, and RoadProfile.flat().ramp_gradient at the same world point exactly (0, 0) - §2.3's byte-lock argument; THE BANK: ramp_gradient at 414785755-0 chainage 76 is 0.3000 to the right of travel (the drape's crossfall +0.30), 0.0000 along; BODY ATTITUDE (open question 4, measured, not judged; the decision is the Conductor's): stood on the Karussell bank - pitch -0.00°, roll 0.3126 rad = 17.91° for a 30.0 % cross-slope whose true angle is 16.70°, the small-angle gap 1.21°, the body 0.002 m under the road's centre height (the origin's ride height, the CG offset), four wheels carried; stood on the steepest stretch - pitch 0.1595 rad = 9.14° for 15.8 % whose true angle is 8.98°, gap 0.16°, roll -0.88° for -1.5 % across, the body 0.050 m under the centre's height by the pitch times the centre of mass's offset; nothing dangerous: the model's tan-for-angle overstates by 1.2° at 30 % and 0.2° at 16 %; DETERMINISM: a second scene instanced fresh and driven the same 2 km lands on the same odometer 2000.036839 m and the same position to the bit (the harness adds each scene at the start of a physics step: added in idle time the car's first tick comes a frame later and a run's odometer carried one idle tick more of pit-lane creep - 0.5 mm - than the other's; found by a per-tick trace, the drives themselves were identical throughout).
- tests/menu_test.gd (additive, the row-shift cascade): Garage.MAPS.size() was 1 -> 2 (the Ring's id and scene asserted), the DRIVE page's rows was 1 + tests + 2 -> 2 + tests + 2, the test rows was from row 1 -> 2, L0 was row 6 -> 7, the skid pad was 7 -> 8, the test-2 activation was row 2 -> 3, the map rows' hints (the pad's start line, the Ring's pit area); the pad's free-drive row asserted to change no scene; the Ring row asserted to close the door and change the scene for real (current_scene the Ring, its road built with 3 304 roads, its car a fresh ArcadeCar on a WorldRoadProfile, the hand-added main scene still in the tree), then unload_current_scene() and the pad scene checked intact; the SETTINGS page asserted to carry the OSM string.
- run_tests.sh: the one step ("ring drive test", after the world profile test's line, with --fixed-fps 60) and the header's enumeration and "seventeen tests"; tests/README.md's enumeration, the count and the note that the ring test writes nothing under /tmp; README.md's Tests prose gains the step in house style. Docs pass (ruling 3): data-pipeline.md's two citations naming 355_5580 as the Karussell's tile "was ->" 356_5581 (the §8 metadata-XML citation of that tile's licence text is a real file, left); ring-region-decisions.md §4 gains the pit anchor line.
- KNOWN ISSUES left for the Conductor: (1) the ten crossing structures unbuilt (above); (2) the blend band beside a lifted approach still eases into the RAW lattice, which holds the DGM hole, so a small lip can stand at a hole's mouth off the paved width until the terrain pass (no terrain mesh this iteration; cosmetic, approved); (3) the mesh at kinks and junctions follows a discontinuous field (up to 197 mm across a bisector on the loop, metres where a side road's centreline is nearer at a junction) - the field's single-valuedness, 4B-3's known issue, now visible in the mesh; the strips of consecutive segments meet unmitred at their shared junctions (a visual seam); (4) the follower floor lags the car by one tick (0.018 m per tick measured at 17 m/s; 0.1 m of height at 30 m/s on 20 %, inside the 0.12 m clearance; a faster car on a steeper descent would touch it); (5) the largest step left on the loop's field, 0.41 m at the T13 junction 65385912 where the hole's floor meets the junction roads' honest ground; (6) the Boxengasse continuation at Hohenrain east (the amendment's straighter-branch case) lifted 2 m of the GP pit lane rather than the loop's own 41395670-1 - bounded and honest, the spike gone all the same; (7) body attitude on the 30 % bank overstates the roll by 1.2° (tan for the angle) - open question 4 stays the Conductor's.
- The codex read-only cross-review of 4B-4 (gpt-6-astra) found real boundary mismatches against the endorsed rule text and the test's claims; fixed forward as a single commit, the 4B-3 precedent (80b3917), nothing frozen touched, drape.json the same bytes: (F1) at a two-segment junction the rim walk returned the other covered untagged segment BEFORE the heading check, so a hairpin would have been walked through - every junction's candidates now go through the same ≤ 60° best-continuation filter, whatever the arity; re-measured, NO rim moved (every two-segment junction the loop's walks pass is within the turn: Breidscheid, Döttinger Höhe, T13 and every off-loop lift the same to the centimetre); (F2) an end whose walk found no rim kept its abutment as the deck line's end, against "nothing where no rim is found" - a lift now needs a rim FOUND at both ends (a rim at distance zero, the abutment its own rim, is one), and two abutments that are both their own rims draw no line (there is no hole; the line between them would have lifted 14 decks by a rounding's 2-9 mm, 41395681-0 among them): one bridge moved, OFF the loop - 134220315-0 (its start rim 4.51 m out at 557.43 m, its end unfound) had 46 stations lifted by up to 1.505 m onto a line drawn to the unfound end's abutment, and is now left as the file has it; 15 of the 33 covered bridges lifted, 42 records replaced (was 16 / 44), every loop bridge's numbers unchanged; (F3) _twist_pieces clamped the tolerance's piece count to floor(interval / MIN_SECTION_M), so a short interval with a large crossfall swing could exceed the 1 mm twist bound (the review's counterexample: a 0.05 m interval on a 4 m half width, crossfall 0.02 → 0.03, needs 10 pieces, the clamp returned one, 10 mm left) - the count is now the tolerance's alone, an interval split finer than MIN_SECTION_M counted in fine_split_count and reported: the Ring's build has 129 such intervals, 330 094 sections (94 038 added by the twist bound; was 329 076 / 93 020), 990 282 vertices, 1 307 160 triangles, the interior quads still within 1.07 mm, the car's drive and its determinism bit-identical to before (the car reads the profile, not the mesh); the counterexample is the test's additive regression case; (F4) the test's maxima folded NaN silently (NaN > worst is false) and several claimed coverage over possibly empty counts - every maximum now takes finite readings only, with a positive count required where a check claims coverage (the vertices, the interior and kink quads, the loop's own stations, the deck lines, the sweep), and _spike_near reports its qualifying station count and refuses a non-finite reading, each bridge-end line printing the count (9-14 stations); the checked-in data is clean, this is regression protection; (F5) the purity assertions snapshotted only _drape.segments[0] - now the COMPLETE parsed skeleton and drape are serialised before each call and compared after (5 984 548 and 5 480 174 bytes as JSON), and the complete outputs of two independent calls compared (the whole output drape and every lift; the whole output drape and every crossing); (F6) the drive's wheel guarantees were overstated: `not is_airborne` clears when ANY wheel carries, and the paved-width claim was an estimate from the centre's offset + HALF_TRACK + the start road's width - now all four car.wheel_supported flags are checked every tick (0 of 7 078 ticks with one unsupported) and every wheel's contact point (global_transform × WHEEL_CONTACT_POINTS, the seam car.gd's own corner check reads the road through: yaw, axles and track in it) is held inside the nearest loop chord's own paved half width every tick (28 312 readings, the smallest margin 3.130 m inside the edge at 2 027 m along, wheel 3), the two crossing passes asserted present and each held to the same (0 unsupported ticks, margins 3.32 / 3.39 m); (F7) the whole-loop sweep never compared its last sample with its first - the closing seam is now folded into the worst step (0.023 m); (F9) 41395681-0 gains the unchanged-data assertion 41226730-0 has (its dense heights the file's, covered, swept, no lift: both abutments their own rims); (F11) the test's header said Hohenrain's four-way residual was "recorded, not fixed" - it now states amendment 2's outcome (59.3 % → 3.0 % via the straighter Boxengasse branch 769107218-0). Reviewed and correct, left as they are: F8 (the scene wires the profile by runtime injection, not a serialised resource - the corrected field cannot exist as a resource, the builder constructs it and assigns it through the car's setter); F9's other items (the headline attitude numbers printed, not pinned: the Conductor's decision input by design). Gates after: suite 2 100 ok (2 094 + the 6 new lines: 41395681-0 unchanged, the twist counterexample, the fine-split count, the four contact points inside the width, all four wheels carried every tick, the two passes present; 23 existing ring lines reworded with their counts) - three times sequential, runs 2 and 3 byte-identical modulo the fd-3K-licence-<pid> line, --parallel the same ok lines in the same order and the same verdict, certs byte-locked (cmp exit 0 in all four runs), error grep 0, the frozen-path diff empty, drape.json sha256 b8d4e531… unchanged, the git-archive copy of HEAD the same verdict.

## ISSUE FLAG (2026-09-23): THE DRIVER'S COMPLAINT CHANNEL, LANDED
A small standalone driver-tool iteration (3-series style) built and gated by the external job on top of be29c54: the canon's "while driving: START a flagging session (input action); the (already persistent) telemetry gets an ISSUE ID bound to the session/time-range; drive around demonstrating the problem; STOP the session; type a text description of what is wrong. Later the driver tells the Conductor 'analyse the issues' and the store + referenced telemetry sessions get read and diagnosed." Nothing frozen touched (car.gd, telemetry.gd, mission_manager.gd, garage.gd, odometer_store.gd, both scenes, every existing test): the tool rides the HUD, the one editable node both maps share that holds the car.
- scripts/issue_store.gd (NEW, IssueStore extends RefCounted, OdometerStore's style: PATH user://issues.json, VERSION 1, static functions, DataDir.resolve on every read and write, a path override on every call, per-field problems in words) - {"version": 1, "next_issue_id": N, "issues": [...]}; a record: id issue-NNNN (the file's own counter, four digits at least, never reused), status open, description, started_at / stopped_at (WALL CLOCK, the telemetry's file-name / started_at idiom: for finding a drive, nothing measured against them), duration_s (physics ticks × 1/60, the same drive the same number), binding ("telemetry" | "odometer+wallclock"), session_id, t_start_s / t_stop_s, odometer_start_m / odometer_stop_m, car_state {x, y, z, heading_deg, speed_ms, gear, odometer_m} snapped like a sample (VALUE_SNAP 0.001). enabled() = TelemetryRecorder.should_record(); allowed(path) refuses the DEFAULT path, read and write, while off - a path of one's own is read and written as told. load_issues() checks field by field ("path: issue issue-0009's status is no status (weird), they are open, closed, open is used"), leaves out a record with no usable id by position, brings a counter behind the ids up past them; add() writes the record under its own id if free, else the counter's, every other field checked and defaulted, the rest of the file written back as read. (A first draft named the reader `load`; that is the global resource loader inside a static function - renamed load_issues.)
- scripts/issue_flagger.gd (NEW, IssueFlagger extends Node, made in code by the HUD's _ready - the mission manager's idiom for the recorder; no autoload: the project has one and no pattern for more): polls Input.is_action_just_pressed("issue_flag", exact) in _physics_process, never the car's input path; start() takes the id from the store's counter, the car's snapshot, the odometer and the wall clock, and finds the recorder; stop() freezes the record with its range and hands it out (signal stopped); describe(text) files it through IssueStore.add on the flagger's path (default PATH, gated); active_id / pending / last_filed / last_written_id for the HUD and the tests; signals started / stopped / filed.
- THE TELEMETRY BINDING, honest on both maps: the recorder is FOUND at every start under the HUD's parent (the scene root: main.tscn's MissionManager makes it in ITS _ready, after the HUD's, so a lookup at the HUD's _ready would find nothing; eifel_ring.tscn has no MissionManager and no recorder), by class check, recording. Found: the record carries THE RECORDER'S OWN _session_id (its session_start line's and its file name's number; 0 in a debug record_to_file recording, honestly) and the range in THE RECORDER'S OWN CLOCK - its _session_ticks through its own _seconds(), the samples' t_session_s, read at the start tick and the stop tick; the HUD stands before the MissionManager in main.tscn, so the flagger's tick runs before the recorder's and what it reads is the second of the sample written that tick. Not found: binding "odometer+wallclock", session_id 0, both seconds 0.0 - the telemetry explicitly absent -, the drive identified by odometer_start_m .. odometer_stop_m and started_at .. stopped_at. Either way the odometer range, the wall clock and the tick-counted duration are written. The canon's "later ... read and diagnosed" stays satisfiable: on the pad the record names the session id and the second range inside the .jsonl; on the Ring the odometer + wall-clock range identifies the drive.
- THE DESCRIPTION UX - chosen for the flagging tool: HUD LineEdit overlay (garage row blocked by frozen menu_test, which pins the garage's row counts). scenes/hud.tscn gains four nodes only: IssueLabel (over the odometer line, bottom right: "ISSUE issue-0007  recording  -  V stops it and asks what is wrong") and IssueOverlay (PROCESS_MODE_ALWAYS - the garage's own mechanism: SceneTree.paused with the overlay alone kept processing, so its keys reach it; 880 × 104 px in the middle of the 1280 × 720 frame: Back, Caption, Description LineEdit, max 400 characters). Flow: V starts, the line goes up; V stops, the line goes down, the overlay comes up with the id and the binding spelt out ("bound to telemetry session 7, 812.5 - 837.5 s" / "no telemetry recording here: bound to the odometer, 12345.7 - 12960.1 m, and the clock"), the box focused, the tree paused; Enter files the text, the tree runs on. ESC - THE EXACT BEHAVIOUR CHOSEN: Esc files the issue AS TYPED SO FAR ("" for nothing: the record stays filed and open, the drive is the evidence), and THE GARAGE THEN OPENS ON THAT SAME ESC. Measured, not assumed: the garage polls Tab and Esc through the pause (PROCESS_MODE_ALWAYS), and a key the LineEdit has taken cannot be taken back from Input - is_action_just_pressed holds for the frame whatever accept_event or Input.action_release say (Godot 4.7, legacy_just_pressed_behavior off; probed headless before the design was fixed). So the overlay files as typed rather than discarding (text typed for a minute is not thrown away for a key the overlay cannot keep to itself), the garage comes up as it does on any Esc with nothing running, and Tab or Esc closes it; as a backstop the HUD finds the Garage beside it by name (both frozen scenes name it Garage; a bare HUD has none and wires nothing) and its `opened` files what is typed and takes the overlay down, the garage holding the pause from then on. The test pins the whole sequence: Esc → filed as typed → garage open → Tab closes it, the tree running on.
- THE KEY - chosen for the flagging tool: V (physical 86), the driver's voice. The brief's first candidate H (72) measured NOT free: Godot 4.7's built-in ui_filedialog_show_hidden is a PLAIN H (keycode 72, no modifier - the probe printed it), which is how the test's conflict check came to walk the engine's built-ins as well as the game's actions. The free plain letters were J, O, P, U, V, Y, Z; V's one modified use is the engine's ui_paste (Ctrl/Cmd+V), which pastes into the box, and the flagger matches its key exactly so Cmd+V flags nothing. project.godot gains exactly one action, issue_flag, nothing else changes; the flagger's poll is exact-match (Input.action_press in the tests is exact, so the taps still land).
- tests/issue_flag_test.gd (NEW, 58 ok, tmp /tmp/fd-3IF-issue-<pid>/ removed at the end): the key in the map and on nothing else, plain, built-ins included; the store off headless, the default path refused to read and to write, a session on the default path writing nothing (user://issues.json's resolved path file_exists false after, read-only); the line up with issue-0001 on the key, the overlay up on the second press with the caption, focus, the pause and the car frozen 30 ticks with the throttle down (position, velocity, odometer, rpm, gear identical), the key doing nothing while the box is up, the text typed character by character through Input.parse_input_event (a plain v types a v), Enter filing it; then on the test's own file the three bindings against the REAL recorder main.tscn's mission manager makes: record_to_file → session 0 honestly, the range == the recorder's _seconds(_session_ticks) at the two signals and its length == the flagger's tick count to 1e-5 s, the car's state == the car's at the start read by the test in the same tick; Esc → filed as typed, the garage open over it, Tab closing it; _session_id = 42 set on the recorder (test-only) → the record carries 42; the recorder stopped → odometer+wallclock, session 0, 0.0 - 0.0 s, the same keys the same ticks apart (duration to the tick of the session before); the three records read back to the bit, counter 4, the id format pinned, sixteen faults in a fixture each named with the default used, records without an id left out by position, a counter behind the ids brought up, an id already taken re-issued and a free one kept; a bare hud.tscn in a Node3D of its own (the Ring's case) with the main scene's recorder recording all the while: not found, odometer+wallclock, filed to its own file. tests/run_tests.sh gains exactly one step ("issue flag test", after the menu test, before the airborne test) and "seventeen" → "eighteen" in the header and the --parallel help; tests/README.md and README.md (the controls row, the tests paragraph, a "The issue flag" section after Telemetry) gain their rows.
- Known limits, honest: (1) the ControlsLabel in hud.tscn and the garage's CONTROLS_TEXT (frozen) do not list V - the README does; (2) a description is one line of up to 400 characters; (3) nothing closes an issue yet (status stays "open": the Conductor's analysis pass is the reader); (4) the description box is a LineEdit: Cmd+V pastes, Tab opens the garage over it (filed as typed by the backstop).
- GATES: suite 2 158 ok (was 2 100: +58, the new step's lines; no existing line changed) - twice sequential, the two logs byte-identical modulo the fd-3K-licence-<pid> line (diff: that one line, 6876 vs 7221), --parallel exit 0 with the same 2 158 ok lines in the same order, certs byte-locked (the 'metrics:' lines cmp exit 0 against /tmp/fd-4BPREP-cert-base.txt in all three logs), SCRIPT ERROR / ERROR: / Parse Error grep 0 in every log, the frozen-path diff be29c54..HEAD empty (car.gd, telemetry.gd, mission_manager.gd, garage.gd, odometer_store.gd, both scenes, every existing test, tools/, configs/, data/), the git-archive copy of HEAD 7d9d52f exit 0 with the same 2 158 ok lines (modulo the pid line), errors 0, cert cmp 0; the no-write proof: user://issues.json's resolved path file_exists false after a session on the default path (the test's read-only check) and the real user data folder (~/Library/Application Support/Godot/app_userdata/Factory Driver/) listed without it before and after; user-thoughts.org never staged.
- The codex read-only cross-review of the issue flag (gpt-6-astra) found four real things - one hole in the flagger, two in the store, three weak claims in the test -, re-verified by the orchestrator against the files; fixed forward as one commit on top, the 4B-4 precedent (be29c54), nothing frozen touched (b347a38): (F1) the flagger's stop read whatever clock the recorder held at that moment, so a recorder RESTARTED between the two keys gave the OLD session id the NEW session's endpoint (two sessions in one range) and a recorder FREED gave t_stop_s 0.0 - a range ended before it began - while the function's comment claimed "the range then ends where it began"; now _stop_seconds reads the recorder's clock at the stop only while the object is valid, still recording AND under the session id written down at the start, else t_stop_s = t_start_s (the range honestly ends where it began, the duration in ticks still counted, the binding still "telemetry"), the comment made true and widened (two debug recordings, both id 0, cannot be told apart - recorded); (F2) the store's bounds: an id's digits capped at ID_MAX_DIGITS 12 (to_int saturates at 19 digits and highest + 1 overflowed: "issue-9223372036854775807" is now no id, the existing "has no id" drop), COUNTER_MAX 99999999 (the counter is eight digits at most; highest clamped to it, a next_issue_id must lie within highest + 1 .. COUNTER_MAX or the existing "is no counter" text and highest + 1 are used - 1e30 passed the whole-number check and int() mangled it; add from a number past the ceiling writes nothing and returns "", the full file's only report, while a free id of one's own under the ceiling is still written), SESSION_ID_MAX 2147483647 (an int32: "is over 2147483647 (3000000000.0), 0 is used") and GEAR_MIN -1 / GEAR_MAX 20 ("is under -1", "is over 20": the ceiling only keeps a corrupt number out), each the existing pattern - a problem text and the default; (F3) load_issues took two records under one id silently - the ids seen are tracked by number, the first record keeps its id, a later one under it is left out with "issue N's id issue-0001 is already in the file, the first record keeps it, this one is left out" (the no-id drop's idiom), add's `taken` keyed by the id's number so it stays correct; (F4) the test, additive: the Esc sequence was asserted on an EMPTY box, which a discarded text would have passed too - "snaps over the crest" is typed first and the filed description asserted equal to it (was -> Esc files what is typed ("") -> ("snaps over the crest", typed into the box first)), the Tab line reads that text back from the file (was -> "Tab closes the garage, the tree runs on" -> "...; the text Esc filed is in the file"), and an anchor outside the recorder's own fields: the debug recording's .jsonl parsed, its last sample's t_session_s inside t_start_s .. t_stop_s (2.00000 s inside 0.10000 - 2.20000 s); new checks: the recorder stopped mid-flag (telemetry, session 0, 0.08333 - 0.08333 s, 124 ticks) and restarted mid-flag as session 43 with its clock at 0.03333 s below the start's 0.08333 s (the record keeps session 42 and the degenerate range, filed and read back with no problem text), twelve digits count and thirteen do not, the bounds fixture (five texts named), the full file, the duplicate id (add under it writes under the counter's issue-0003, the file's own duplicate written back as read and still left out). The default-path nit the orchestrator judged covered: skipped. Known issues recorded, not fixed, both in files this iteration may not edit: the telemetry's pruning (SESSIONS_KEPT 20, telemetry.gd) can delete a referenced session's files before an issue is diagnosed; issues.json is not in the data folder's seed list (DataDir.SEEDED_FILES is cars.json alone). Gates after: suite 2 170 ok (2 158 + the 12 new lines, the two cited claims the only existing lines changed) - three times sequential, runs 2 and 3 byte-identical modulo the fd-3K-licence-<pid> line (run 1 carries the import step's re-registration block for the two edited classes, its ok lines the same), --parallel the same ok lines in the same order and the same verdict, certs byte-locked (cmp exit 0 against /tmp/fd-4BPREP-cert-base.txt in all four logs), SCRIPT ERROR / ERROR: / Parse Error grep 0 in every log, the frozen-path diff 2c290da..HEAD empty, the git-archive copy of HEAD exit 0 with the same 2 170 ok lines (modulo the pid line), errors 0, cert cmp 0; user-thoughts.org never staged.

## ROAD-SMOOTHING (2026-09-23): THE DRAPE'S FEATURE-PRESERVING REFINEMENT AND THE JUNCTION STITCH, LANDED
The driver's first Ring drive filed five issues (user://issues.json via the issue flag); this iteration answers issue-0001 "the road tile is very pointy, very un-natural ... maybe we need to try to increase the resolution from the data that we have, or have a better way of creating the terrain from the data that we have" (car at x 2017.926 z -1373.37) and issue-0005 "this is a place where two tiles of the road connect, but one is higher than the other, and here, at this side of the road, it's like a stair ... maybe we can smartly add more resolution to the existing data, like filling in the gaps that the data that we have doesn't provide. this should be a rule for all data, including osm for the open world outside greenhell" (x 1594.015 z -1218.086), and diagnoses issue-0002 "there is something that appears as a big whole in the road" (x 714.38 z -1782.091, reverse, standing). The research survey's recipe as the Conductor adjudicated it, its numbers verified on the data. Write-side only: tools/world/drape.py and the regenerated data/regions/eifel_ring/drape.json; the readers (world_road_profile.gd, road_builder.gd), the skeleton, the scenes, the configs and every frozen test untouched.
- WHAT THE DATA SAID FIRST (measured before anything was written): the file's centre heights already agreed at every junction (the same DEM sample: 0 height gaps over 2 158 junctions with two or more covered ends) - the driver's stair is not a height. It is the CROSSFALL: each segment's end point took its own bend's superelevation, so at junction 65386044 (issue-0005) 1009142895-0 ended at -0.77 % and 799394496-0 began at +4.0 % on one centre height 585.23 m, a 0.255 m step at the right paved edge and 0.150 m at the left; 1 562 junctions had a crossfall gap over 2 %, 29 of them on the loop. Issue-0001's car stood 3.4 m from the T13 junction 312821860 where the pit lane 199642470-0 joined the loop at -4 % against the loop's +4 %: a 0.340 m ridge at the paved edge - "pointy". The DGM1 on the paved loop is far smoother than the survey's F1 noise model (±15 cm per cell): the 4-16 m band reads 5 mm rms, the station-to-station grade change 1.5 % at the 90th percentile and 3.0 % at the 99th (the forest tracks are the noisy class: 5 % and 17.5 %). The T13 four-way junction's 0.413 m step in the ring drive test's loop sweep was never in the file either (the four ends read 618.65 m alike): it was the reader's rim rule lifting one branch through the junction onto 769107218-0 while 41395670-1 kept the hole's height.
- THE SMOOTHER - chosen for the smoothing: Whittaker penalised smoothness (Eilers 2003, "A Perfect Smoother"), z = argmin Σ wᵢ(yᵢ − zᵢ)² + λ Σ(Δ²z)², per covered plain segment on the raw dense centre heights before rounding; bridges keep their linear decks, tunnels their rule, partly covered segments and segments under 20 m (fewer than 11 uniform stations: no 20 m window fits, no label, all edge) are left as sampled - 2 623 of the 3 314 covered segments smoothed. The recipe's primary, Savitzky-Golay (window 11, order 2), needs scipy; scipy is MEASURED ABSENT from the venv that builds the file (/Users/kokos/.claude/jobs/a61f3c60/venv: rasterio 1.5.1, numpy 2.5.3, pyproj 3.8.0; pip list - rasterio depends on numpy, affine and attrs; no network, nothing installed), so the recipe's pre-approved alternative is the deliverable, in plain python: the pentadiagonal system W + λD'D factored by a banded Cholesky in python floats, no BLAS (a threaded LAPACK solve can order its sums differently run to run; the file has to be the same bytes twice), verified against numpy's dense solve within 2.6e-12 m unweighted and 4.1e-9 m with a run held at weight 1e6. The edge: natural (free) ends, D of n − 2 rows; the station past the last whole one (an uneven length) is outside the uniform spacing and takes the fit's last grade over its own length (found on the way: a 0.3 m tail treated as a 2 m station stepped 8 cm, a 27 % kink at 683303208-0's end that the drive's follower floor caught as one tick over its bound).
- λ = 5, a hardcoded constant - the λ range rescaled for 2 m stations (h⁴ scaling), Conductor-approved 2026-09-23; was the recipe's per-1 m-sample 1e2..1e5. The Whittaker cutoff scales as L_cut ≈ 2π·h·λ^(1/4), so an equal cutoff needs λ ∝ h⁴: the recipe's range at ~1 m samples is L_cut ≈ 12.6-126 m and at 2 m stations the same numbers give L_cut ≈ 39-126 m, attenuating the very 20-40 m crests the gates require to survive. Measured on the checked-in file: at λ 1e2 the transfer H is 0.51 at 40 m and 0.06 at 20 m, 258 labels of |curvature| ≥ 0.01 (2.5 × the threshold) vanish and loop heights move by up to 2.47 m; at 1e4 everything under 100 m is gone. λ 5 gives L_cut ≈ 19 m (18.4 m half-power: 2 − 2cos ω = 1/√5): H = 0.012 at 4 m, 0.048 at 8 m, 0.37 at 16 m, 0.58 at 20 m, 0.87 at 30 m, 0.95 at 40 m, 0.99 at 60 m - what the 20 m label window cannot see is removed, what it sees is kept. The brief flagged it as a guess and asked its numbers verified; a rule that fails its own gates is mis-scaled, not sacred.
- THE PROTECTION - chosen for the smoothing: the labels are taken on the smoothed heights (rounded as the file rounds); every station of a crest/dip run plus one flank station either side is held to its raw height by a weight of 1e6 in the same solve (the "skip smoothing on those runs" branch done inside the fit, so the neighbours bend onto the raw stations instead of stepping to them), and the file's labels are recomputed on the final heights - exactly what tests/world_profile_test.gd's recount reads (it agrees on all 3 314 covered segments). Labels 2 551 crests / 2 465 dips -> 1 927 / 1 874: 1 133 of the labels gone were under 0.006 (the threshold's edge), 113 between 0.006 and 0.01, and of the raw labels of |curvature| ≥ 0.01 seven have no label of their kind within 6 m afterwards - five still labelled with the run's steepest station moved 8-20 m along (395588220-0 at the DEM's cut at a structure's end; 684087028-1, 699271314-1, 826478005-2, 832287291-0 on tracks), two one- or two-station spikes on forest tracks (41795618-0's 412.71 between 412.17 and 412.18, 507849425-1's knee); none a crest of a road, none on the loop; 103 labels appear where a run split or moved, none on the loop. The STOP condition (a real labelled crest the protection cannot keep) did not fire: T13's top, the loop's highest sample 627.52 m at 1009142894-0 chainage 10, IS a labelled crest and is held to the raw height, its 20 m amplitude 2.275 m before and after; the pit lane's crest at chainage 10 likewise (2.330 m).
- THE JUNCTION RULE - chosen for the smoothing, write-side (the brief's blend, extended to the crossfall because that is what the stair was): at every skeleton junction the draped segments' ends on the node are stitched. Height: a rigid participant holds the node - a bridge's deck end, a tunnel's portal, a partly covered segment's raw sample (all the raw ground at the node); else the highest road class wins (CLASS_RANK: raceway, primary, primary_link, secondary, secondary_link, tertiary, tertiary_link, unclassified, residential, living_street, service, track), the Nordschleife loop wins ties, the winners' mean is the node's height; every covered plain participant's stations within 8 m of the node are shifted by smoothstep(1 − d / 8 m) × (node height − its own end height): the grade kept, the gap closed, the radius half the segment's length under 16 m so both ends land, the zones never overlapping so the junctions' order does not matter. Crossfall: the same priority over every non-bank participant's end point (the Karussell's bank neither votes nor moves), the winners' mean written to each end point. Measured on the file: 2 196 nodes' heights stitched (5 924 ends blended; the height stitch closes only what the smoother's free ends open: the loop's 184 segment ends moved by at most 2 cm, except the T13 four-way node's two by 8 cm - known limit (2) below), the crossfall stitched at 2 231 nodes (the largest gap closed 0.12); issue-0005's stair 0.255 / 0.150 m -> 0.000 / 0.000 (in the field: 0.004, 0.002 and 0.001 m across the seam at -4 m, 0 and +4 m), T13's ridge 0.340 -> 0.000 m; the ring drive test's loop sweep now reads its largest step 0.160 m at 245166664-0 chainage 16 (was 0.413 m at T13: the rim rule's lift through the junction now lands on the stitched node).
- ISSUE-0002 LOCALISED (diagnosed, not fixed - reader-side): the car at (714.38, -1782.09) stood on the loop's bridge deck 41395681-0 at chainage 24.1, 1.61 m from its centreline, 5.58 m above the terrain lattice under it (the DGM1 is a ground model: the valley floor); the primary 828126276-0 passes 5.96 m under the deck and is one of the ten crossing structures the right of way uncovers (no strip built), and 12 m beside the deck the field is the ground 4.13 m and 5.61 m below it. The hole is the bridge's missing sides and the unbuilt road under it, not a height in the file: the deck is linear and the same bytes as before (41395681-0, "already clean" at both ends). Smoothing and blends change nothing there. Known issue, recorded with the ids: the assembler's bridge sides / abutment walls and the crossing structures (4B-4's known list) are reader-side work.
- THE EVIDENCE OVER ±50 m AT THE THREE SITES (the Conductor's requirement; the raw numbers pinned in tests/world_profile_test.gd, the smoothed ones measured there from the file): issue-0001 - the pit lane 199642470-0 over 0..48 m: the station-to-station grade change's peak-to-peak 0.2675 -> 0.2025 /m (53.5 % -> 40.5 % per station: the T13 bridge hole's wall is inside the window, the rim rule's business), the crest label at chainage 10 kept at amplitude 2.330 m; 1009142894-0 over 0..48 m: 0.2300 -> 0.1575 /m, the crest at 10 (the loop's top, 627.52 m) kept at 2.275 m; 1009142894-1 over 0..50 m: 0.0150 -> 0.0075 /m (3.0 % -> 1.5 %). issue-0002 - the deck 41395681-0: 0.0050 -> 0.0050 (the same bytes); the road under it 828126276-0: 0.0550 -> 0.0050 /m (11.0 % -> 1.0 %). issue-0005 - 1009142895-0 over 416..468 m: 0.0200 -> 0.0050 /m (4.0 % -> 1.0 %); 799394496-0 over 0..50 m: 0.0175 -> 0.0050 /m (3.5 % -> 1.0 %); the stair 0.255 / 0.150 m -> 0.000 / 0.000 m. On the whole loop the grade change's 90th and 99th percentiles 0.0075 / 0.0150 -> 0.0025 / 0.0050 /m (the centimetre rounding's own quantum and twice it); the rms change of a loop height 1.5 cm, the largest 0.74 m on the Döttinger Höhe bridge's east approach (683303211-0 chainage 4, the DGM1's bridge hole the rim rule bridges); the terrain truths the same to the centimetre: Breidscheid 332.94, T13 627.52, Hohe Acht 616.50 m; the loop's length untouched (plan view).
- THE NOISY FIXTURE (drape.py --selftest, numpy: the suite cannot run it, world_profile_test.gd's header cites it): ±15 cm of seeded white noise per 1 m cell (numpy's PCG64 Generator at seed 4) on a plane with a 30 m crest, an 800 m road through it: the flat band's height residual peak-to-peak shrinks 2.4 × (0.211 -> 0.090 m), its rms 0.0425 -> 0.0201 m, the station-to-station grade change's peak-to-peak ≥ 3 ×, no crest/dip label on the band before or after (±15 cm of white noise reaches |0.0013| /m in the 20 m window after, far under 0.004: the DGM1's noise does not make labels; the labels that go on the real file are spikes and the threshold's edge), the crest still labelled with the raw data's own curvature (-0.02800 /m) and its top station the raw height. The brief's ≥ 3 × on the height residual: measured against λ on this white noise - 5 -> 2.4 ×, 10 -> 2.6 ×, 30 -> 3.1 ×, 60 -> 3.1 ×, 100 -> 2.4 × (the crest's smear reaching the band) - the first λ reaching 3 × is 30, where the label rule's own 40 m wavelength is cut to 0.78: the feature gate's loss, recorded, not taken (the selftest holds 2 × on the height residual and 3 × on the grade change). Also in the selftest: the junction rule on synthetic records (a track meeting a primary 0.3 m higher blended by smoothstep over its first 8 m, two loop segments' mean holding a service road, a rigid bridge holding a primary, a partly covered segment holding its raw sample, a 10 m track landing on both of two primaries 0.4 m apart, the crossfall's mean at a node with the Karussell neither voting nor moving, purity), the solver against numpy's dense solve, a line left alone, a held run, a segment under 20 m left raw, the uneven end.
- TESTS (additive; every changed expectation cited was ->): tests/world_profile_test.gd +12 ok (112 -> 124): the loop's grade change at the 90th / 99th percentiles ≤ 0.0025 / 0.0050 (was 0.0075 / 0.0150 pinned), every junction's draped ends on one height and one crossfall (2 199 / 2 231 nodes; was 1 562 gaps), issue-0005's stair from the file (0.000 / 0.000, was 0.255 / 0.150) and in the field (≤ 3 cm across the seam), issue-0001's T13 ridge (0.000, was 0.340), the seven ±50 m windows with the two crest labels' amplitudes kept within 0.011 m; SAMPLES_DIGEST was b167c232... -> bab692ef... (the file's heights changed, the arithmetic did not). tests/ring_drive_test.gd +1 ok (64 -> 65): issue-0002's localisation; DRAPE_SHA256 was b8d4e531... -> fa2dfb58...; ONE THRESHOLD MOVED, flagged: the share of the loop's quads a vertex or a probe of which reads another road, 5 % -> 10 % (OTHER_ROAD_QUADS_SHARE_MAX, was inline 0.05): the crossfall stitch puts a crossfall ramp on every segment's end chord and the twist bound splits a ramp of 8 % into ~85 sections whatever its length (RoadBuilder._twist_pieces: the count is |Δe| × half width / 4 / 1 mm), so the quads at the loop's own seams - counted there as "another road", the next loop segment - went 925 of 21 673 (4.3 %) -> 1 593 of 22 721 (7.0 %); a mesh-density count, the field the same single-valued one; the build 3 304 roads, 383 146 sections (144 048 added by the twist bound), 1 149 438 vertices (was 329 394 / 94 038 / 988 182). Breidscheid's rim pins unchanged (8.44 m / 338.76 m, 8.00 m / 337.00 m, -5.14 %: the walls softened from 64-65 % to 43-53 % over the same stations, the first station climbing under 20 % the same); the abutment spikes before the rule now read 65 / 57 / 32 / 43 / 46 / 62 / 61 % (the smoothed walls; was 64 / 32 / 61 / 52 / 59 / 54 on the raw file), after 5.1 / 5.1 / 5.1 / 6.8 / 4.0 / 3.2 / 3.0 %; 13 of 33 covered bridges lifted (was 14: one approach's wall now climbs under 20 % on its own); the drive 2 000 m in 118.0 s, all four wheels carried every tick, the follower floor's worst step 0.017 m per tick, two scenes to the bit. No new run_tests.sh step. The certs byte-locked (the pad's profile is untouched).
- DETERMINISM: (a) two builds byte-identical - drape_new5 and drape_new6 cmp equal, 5 399 681 bytes, sha256 fa2dfb58bdd51d1b6b42be1ab3da5fe3ea472c2526d75182ec846269b295fbe0 (was 5 480 097 bytes, b8d4e531...: the smoothed heights carry fewer digits); (b) re-derivation from the verified tiles: the OLD file re-derived first with the unchanged tool to b8d4e531... byte-identical (the venv and the tiles are the pinned ones), the new file the same bytes from the same 42 sha-verified tiles and the untouched skeleton (skeleton.json sha unchanged in the file's snapshot). PIPELINE_VERSION stays 1: the frozen reader refuses another (DRAPE_PIPELINE_VERSION), and the file's `rules` cannot gain a key (validate refuses other rules); the smoothing's constants are recorded in drape.py's header and data-pipeline.md §5, the file pinned by sha256 in the ring drive test. drape.py's docstring now names the real venv path and that scipy is absent.
- KNOWN LIMITS, honest: (1) the DGM1's cuts at structure ends on side roads - a wall of 2-4 m over one station where a road meets a tunnel portal or a bridge abutment whose OSM way ends short - are smoothed into a ramp over ~8 m and then blended back onto the rigid structure's raw end (159029005-1's end: 544.33 -> 548.56 over 2 m became 544.68, 546.06, 548.01, 549.93 over 8 m; the largest end offset 3.08 m); not drivable before, not drivable now, the crossing structures' business; (2) the natural end on a segment whose first stations climb a bridge hole's wall overshoots the raw by up to ~16 cm (41395670-1's start), and the junction's mean carries it to the node (T13's four-way node 618.65 -> 618.73 m), inside the rim rule's lift zone; (3) the loop's largest station kink after the rule is 0.295 /m at 41395670-0 chainage 4 (the T13 hole's wall, the rim rule's); (4) the stair between the Karussell's bank (0.30) and its neighbours' crossfall is the bank's own and unchanged (1.31 m at 4 m offset across 414785755-0 -> 414785756-0 in the field: the bowl's outside edge), the bank being excluded from the stitch; (5) the crossfall ramp on a short end chord can be steep (the pit lane's -4 % -> +4 % over its last 6 m: 34 cm of edge height over 6 m) - continuous, the twist bound holds it in the mesh, but a longer ramp needs skeleton points the writer cannot add; (6) the centimetre rounding's own quantum (0.5 % of grade change per station) is now the loop's floor of pointiness; a millimetre file would be ~1.3 MB larger and was judged not worth it here.
- GATES: suite 2 183 ok (was 2 170: +12 in the world profile test, +1 in the ring drive test; the two cited pins - the samples digest and the drape sha - and the one flagged threshold the only existing lines changed) - twice sequential on top of 3460204 + this tree, the two logs byte-identical modulo the fd-3K-licence-<pid> line and run 1's import-step class re-registration block for the two edited test scripts; --parallel exit 0 with the same 2 183 ok lines in the same order (modulo the pid line); certs byte-locked (the 33 'metrics:' lines cmp exit 0 against /tmp/fd-4BPREP-cert-base.txt in all four logs: SLALOM 28.65 / SPIN_180 15.95 / SPIN_360 18.07 as before); SCRIPT ERROR / ERROR: / Parse Error grep 0 in every log; the archive copy - the working tree as it will be committed, written out through a temporary index (HEAD plus this iteration's seven files, the real index untouched, user-thoughts.org never staged) and git archive into a scratch folder - exit 0 with the same 2 183 ok lines (modulo the pid line), errors 0, cert cmp 0, its drape.json sha256 fa2dfb58...; drape.py --selftest 41 ok (was 22); the frozen paths' diff empty (scripts/, scenes/, configs/, project.godot, skeleton.json, every other test, run_tests.sh); files changed: tools/world/drape.py, data/regions/eifel_ring/drape.json, tests/world_profile_test.gd, tests/ring_drive_test.gd, README.md, docs/design/4b/data-pipeline.md §5, this addendum. Not committed: the orchestrator audits and commits.

## ROAD-SMOOTHING POST-REVIEW (2026-09-23): THE CODEX CROSS-REVIEW'S EIGHT FINDINGS, FIXED FORWARD
The codex read-only cross-review (gpt-6-astra) of the landing above found eight real things, the orchestrator re-verified them against the files (F1 re-proven by independent edge arithmetic: 18.2 cm of world-side edge gap at node 3183494700), the Conductor approved the fix-forward on the b347a38 precedent. The solver, the recount agreement (all 3 314 segments), the two-build reproducibility and λ = 5 were not in question and are unchanged; the heights and the labels of the file are byte-for-byte the first landing's (every one of the 3 479 records' dense heights identical, 1 927 crests / 1 874 dips): only crossfalls were re-tilted and numbers re-measured.
- F1 (data): the crossfall stitch wrote the same SIGNED value to every participant, but a crossfall is "rise to the right of travel" and right is each segment's own frame - two segments leaving a node in opposite directions with one written value carried two opposite tilts; measured at node 3183494700 between the primaries 312490275-0 and 82512875-0 (both starts, right normals near-opposite, both written -0.0259): 0.22 m of gap between the two platforms at one world point 4.25 m out (codex's 18.2 cm by its own arithmetic). FIXED: each non-bank participant's end crossfall becomes a world-space tilt vector (cf × the end chord's right normal, end_right_normal walking inward past duplicate points); the target is chosen in world space; each plain non-bank participant is written the target's component along its own right normal (312490275-0 now -0.0008, 82512875-0 +0.0008: one tilt, the gap 0.000). drape.py --selftest: two primaries leaving a node east (+0.04) and west (0) get +0.02 and -0.02.
- F4 (data): the crossfall stitch had no rigidity - lower-class voters moved a partly covered track's end from 0.06 to 0.0055 at node 1301831393 (440567173-0) and a partly covered service road's to 0.06 at 1107337774 (130743278-1). FIXED to mirror the height rule: a rigid participant (bridge, tunnel, partly covered) holds its own end tilt and is never written; when several are rigid, rigid_pick takes one deterministically - class rank, the loop, the segment id, the start before the end (recorded in §5); else the class/loop winners' mean of the plain participants' tilt vectors; only plain non-bank participants are written. 440567173-0 holds its own 0.0600 and 130743278-1 its own 0.0174 now. tests/world_profile_test.gd's mirror of the rule (every plain end's written value against the target recomputed from the file - the ends' pre-stitch tilts read back from their neighbour points, the rule's own crossfall_of convention - within 2e-4) caught one more thing on the way: the pipeline's pick key sorted an end index -1 before 0, the END before the start, against the documented rule; fixed (one node changed, 476639698, where the partly covered 429774880-0 has both ends on the node). The mirror passes on all 6 172 plain ends at 2 194 nodes (199 held by a rigid participant), the worst miss 0.00008.
- F2 (latent crash): a zero-length record (the skeleton admits identical points) had radius 0 and d / radius raised ZeroDivisionError when its end needed shifting. FIXED with a guard: no radius, nothing written, nothing counted; the crossfall stitch skips its undefined frame. drape.py --selftest reproduces it (a two-identical-point track beside a primary).
- F3 (docs): the recorded λ rescaling argument had its proportionality inverted - L_cut ≈ 2π·h·λ^(1/4) means a constant cutoff needs λ ∝ h⁻⁴, a λ 16 × SMALLER at 2 m than at 1 m, not larger; the recipe's 1e2..1e5 at 1 m gives L_cut ≈ 19.9-111.7 m (not 12.6-126) and at 2 m the same λ range gives ≈ 39.7-223.5 m (not 39-126). Corrected in place with a was -> in drape.py's header and data-pipeline.md §5 (the Conductor's own ruling text carried the inversion; recorded, not blamed): λ = 5 at 2 m (L_cut ≈ 18.8 m) corresponds to λ ≈ 80 at 1 m, just under the recipe's 1e2 floor - its intent's lower edge; the decision stands.
- F5 (docs honesty): §5's "the height stitch closes only what the smoother's free ends open (centimetres)" understated: the loop's 184 segment ends move by at most 2 cm except the T13 four-way node's two by 8 cm, but a side road ending at a structure's cut takes the DEM's wall the smoother turned into a ramp - 3.08 m at 159029005-1's end, the largest (known limit (1)); and "the grade kept" holds at the end station (smoothstep's derivative is zero there) while the offset's own grade spreads over the blend, at most 1.5 × offset / radius at its middle - 58 % per metre on that worst case, 1.5 % on the T13 node's 8 cm. §5 and the header say so now.
- F6 (test method): the grade-change metrics read the uneven end interval as a 2 m one (87 plain loop endpoints; 199642470-0's window read 0.2025 against 0.1775 uniform-only). FIXED: every second difference is taken at uniform stations only (both neighbouring intervals exactly 2 m: k in 1..whole-1); the raw constants re-measured on the raw baseline file with the corrected method: RAW_LOOP_KINK_P99 was 0.0150 -> 0.0125 (RAW_LOOP_KINK_P90 0.0075 the same; the loop's plain stations 10 270 -> 10 183), the seven site windows' raw peak-to-peaks the same seven values (0.2675, 0.2300, 0.0150, 0.0050, 0.0550, 0.0200, 0.0175); the smoothed readings now 0.1775 (was 0.2025), 0.1575, 0.0075, 0.0050, 0.0050, 0.0050, 0.0050 /m; the loop's 90th / 99th percentiles after the smoothing 0.0025 / 0.0050 as before.
- F7 (test honesty): the issue-0002 ok-line asserted the diagnosis as established while checking one side's drop (minf of two probes). FIXED: both sides probed and each asserted over 4 m (4.13 m left, 5.61 m right, 5.58 m under the deck) and the line worded as what is checked (the car on the deck 41395681-0, the primary 828126276-0 5.96 m under it unbuilt, the drops) plus the reading for the driver marked as the reading (the missing bridge sides and the unbuilt road under it).
- F8 (nits): selftest_smoothing's text said the brief's 3 × needs λ ≥ 60 while its own table first reaches 3.0 × at 30 - the text now points at the table; the short plain segments are 648, not 647 (the pipeline's own count: 3 314 covered - 33 bridges - 10 tunnels - 2 623 smoothed), fixed in the header and §5; the noisy fixture's shrink cited both ways - 2.3 × on the finished centimetre-rounded record, 2.4 × on the unrounded fit.
- THE FILE: data/regions/eifel_ring/drape.json regenerated, 5 401 198 bytes, sha256 f3ca142bcc1a3a36ec559889f4d4261a36ee583c4fd52381f64fa09aae64354a (supersedes fa2dfb58..., never committed); two builds byte-identical (drape_new9 / drape_new10 cmp equal), the selftest 44 ok (was 41: the F1, F4 and F2 cases). The crossfall re-tilted at 4 137 end points (the median change 0.0105, the largest 0.1174, 1 084 over 0.02); the crossfall stitched as a world-space tilt at 2 194 nodes (was 2 231 written the old way: a node whose only plain participant has no frame, or none, is now left alone), the largest change written 0.1116 at 690477044-0's end. Pins was ->: DRAPE_SHA256 fa2dfb58... -> f3ca142b...; SAMPLES_DIGEST bab692ef... -> e5b34889... (the re-tilted crossfalls under a few of the 200 on-road samples). Re-measured with the world-space stitch: issue-0005's stair 0.000 / 0.000 m as before (the two loop ends 2° apart carry one tilt, +0.0161 in each frame); issue-0001's pit lane now carries the loop's world tilt (0.0203, -0.0341) in its own frame - crossfall +0.0393, the tilt's component along its right normal at the 0.09 rad join (was +0.0400 written blind; the ridge 0.340 -> 0.0007 m, read as the tilt gap along the pit lane's normal times 4.25 m); the mesh 3 304 roads, 378 093 sections (139 221 added by the twist bound), 1 134 279 vertices, 1 499 156 triangles (was 383 146 / 144 048 / 1 149 438 / 1 519 368 with the first stitch), 1 602 of 22 849 quads reading another road (7.0 %, under the 10 % recorded above), the loop sweep's largest step 0.160 m as before, Breidscheid's rims and every terrain pin unchanged, the drive 2 000 m in 118.0 s to the bit twice.
- GATES (the same battery, on the corrected tree): suite 2 183 ok (unchanged in count: the review's fixes changed no ok-line count, only pins and texts) - twice sequential, the two logs byte-identical modulo the fd-3K-licence-<pid> line and run 1's import-step class re-registration block; --parallel exit 0 with the same 2 183 ok lines in the same order (modulo the pid line); certs byte-locked (the 33 'metrics:' lines cmp exit 0 against /tmp/fd-4BPREP-cert-base.txt in all four logs); SCRIPT ERROR / ERROR: / Parse Error grep 0 in every log; the archive copy - the working tree as it will be committed through a temporary index, every one of this iteration's seven files and the frozen readers cmp-identical to the working tree, its drape.json f3ca142b... - exit 0 with the same 2 183 ok lines (modulo the pid line), errors 0, cert cmp 0 (honest note: a first reading of this battery's results was taken off the PREVIOUS battery's status file - the monitor loop ran before the new battery had removed it - and showed the earlier tree, fa2dfb58's; the battery itself, read again once it had finished, built tree 397565a8 with drape.json f3ca142b... and every file of this iteration cmp-identical to the working tree: the numbers here are its); drape.py --selftest 44 ok; the frozen paths untouched; user-thoughts.org never staged. Not committed: the orchestrator audits and commits.
